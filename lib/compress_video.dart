/// Public API for the `compress_video` Flutter plugin.
///
/// This phase ships the typed error taxonomy every later call throws and maps into. The
/// `CompressVideo` entry point (media info, thumbnails, compression) is added by later plans in
/// this phase and in phases 2-6.
library;

export 'src/compress_video_exception.dart';
