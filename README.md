# compress_video

Drop-in successor to the abandoned `video_compress` Flutter plugin, built on Media3 Transformer and AVAssetReader/AVAssetWriter. Planning lives in `.planning/` (GSD). Research that led here: `.planning/research/sources/VIDEO_COMPRESS_BRIEF.md`.
