---
phase: 03-apple-compression-to-parity
plan: 09
type: execute
wave: 8
depends_on: ["03-08"]
files_modified:
  - .planning/phases/03-apple-compression-to-parity/ios-simulator-compressing.png
  - .planning/phases/03-apple-compression-to-parity/ios-simulator-result.png
  - .planning/phases/03-apple-compression-to-parity/macos-result.png
  - .planning/phases/02-android-compression-on-media3/02-UAT.md
  - README.md
  - CHANGELOG.md
  - doc/TOOLCHAIN.md
  - lib/src/compress_result.dart
  - .claude/CLAUDE.md
  - QUESTIONS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CORE-01, CORE-07, BULD-02, BULD-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The example app has been run on the iOS simulator and on the macOS host through its whole flow — pick, options, estimate, compress, live progress, cancel, result card, playback — and screenshots of the compressing state and the done state are committed into this phase directory (D-27, BULD-04)."
    - "02-UAT.md item 6, the example-app compress screen on a real display, is marked resolved with a reference to the committed screenshots and the platforms they came from, rather than left pending against a headless emulator that could never close it."
    - "One observed GitHub Actions run has both the `apple` job and the `parity` job concluding successfully, and its run id is recorded in the summary — the phase is not signed off on a local-only result."
    - "All four compression suites plus the two Phase 1 suites pass on the Android emulator, the iOS simulator and the macOS host in one final sweep, with the passing case count recorded per platform per suite and any per-platform difference explained rather than averaged away."
    - "The README and the `CompressResult` dartdoc state explicitly what 'the same observable result on every platform' means — the same dimensions, codec, flags and duration, and deliberately different bytes and elapsed time because the encoders differ — so no consumer reads a byte-parity promise this package does not make."
    - "Every requirement this phase owns — CORE-01, CORE-07, BULD-02 and BULD-04 — is checked complete in `REQUIREMENTS.md` only where its evidence exists and is named, with the evidence recorded beside the change in the summary."
    - "Anything this phase could not prove is carried forward explicitly rather than quietly dropped: the unresolved BULD-02 minimum-deployment-target question flagged in 03-08, any audio or error branch with no corpus fixture, any Apple-versus-Android tolerance divergence discovered along the way, and the five remaining 02-UAT items that need hardware or a fixture this phase does not add."
  prohibitions:
    - statement: "MUST NOT mark a requirement complete in `REQUIREMENTS.md` without naming the artifact or the observed run that proves it."
      status: active
      verification: "Each of the four requirement rows changed in this plan has its evidence named in the summary, checkable against a committed file or a recorded run id."
    - statement: "MUST NOT run any `gsd-tools` state, roadmap or requirements WRITE subcommand — this project has a recorded data-loss bug where those writers corrupt planning markdown they never mention."
      status: active
      verification: "`STATE.md`, `ROADMAP.md` and `REQUIREMENTS.md` are hand-edited, and `git diff` for each is reviewed before commit and shows only the intended lines."
    - statement: "MUST NOT claim the phase complete on a locally-green result alone — the CI `apple` and `parity` jobs must both be observed green on one real run."
      status: active
      verification: "`gh run view <id> --json jobs` shows both jobs with conclusion success, and the id appears in the summary."
    - statement: "MUST NOT promise byte-for-byte or elapsed-time parity across platforms anywhere in the README, the dartdoc or the CHANGELOG."
      status: active
      verification: "The parity wording names dimensions, codec, flags and duration as guaranteed and bytes and elapsed time as platform-dependent, in both the README and the `CompressResult` dartdoc."
    - statement: "MUST NOT mark any 02-UAT.md item other than #6 as resolved — the rest need a physical phone or a fixture this phase does not add."
      status: active
      verification: "`git diff .planning/phases/02-android-compression-on-media3/02-UAT.md` changes only item 6's result line and the summary counters."
  artifacts:
    - path: ".planning/phases/03-apple-compression-to-parity/ios-simulator-result.png"
      provides: "Visual proof of the example app's done state on iOS, closing 02-UAT.md #6"
    - path: ".planning/phases/03-apple-compression-to-parity/macos-result.png"
      provides: "Visual proof of the same screen in the macOS desktop layout"
    - path: "README.md"
      provides: "The explicit statement of what cross-platform sameness does and does not include"
      contains: "elapsed"
  key_links:
    - from: ".planning/phases/02-android-compression-on-media3/02-UAT.md"
      to: ".planning/phases/03-apple-compression-to-parity/ios-simulator-result.png"
      via: "the reference that closes the deferred manual item with an artifact rather than an assertion"
      pattern: "03-apple-compression-to-parity"
    - from: "README.md"
      to: "doc/PRESETS.md"
      via: "the link from the parity wording to the measured per-platform tables that substantiate it"
      pattern: "PRESETS"
---

<objective>
Sign the phase off on observed evidence: the app running on both Apple platforms, one green CI run
covering every suite and the parity gate, and documentation that promises exactly what the package
delivers.

Purpose: every prior plan in this phase proved its own slice locally, on a Mac reached over SSH.
The phase's actual claim — that a developer gets the same behaviour on three platforms, and that
the example app demonstrates it — is only true if one run of the real pipeline says so, and if the
visual half that no test can assert has been looked at by someone. This plan is also where the
honest accounting happens: what is proven, what is documented as a known platform difference, and
what is carried forward unresolved.
Output: committed screenshots from the iOS simulator and the macOS host, a recorded green CI run
covering the `apple` and `parity` jobs, the cross-platform sameness wording in the README and the
dartdoc, and requirement rows checked only where evidence exists.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-apple-compression-to-parity/03-CONTEXT.md
@.planning/phases/03-apple-compression-to-parity/03-01-PLAN.md
@.planning/phases/02-android-compression-on-media3/02-UAT.md
@README.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a person watches it work" — the example app on the iOS simulator and the macOS desktop</name>
  <precondition>`bash tool/mac_sync.sh && bash tool/mac_run.sh ios integration_test/compress_test.dart` exits 0, confirming the engine works on the simulator before the app is driven by hand on top of it.</precondition>
  <read_first>
    - `.planning/phases/03-apple-compression-to-parity/03-UI-SPEC.md` → "Screen States" (the seven states, so the screenshots capture the two that matter — compressing and done — rather than whatever happened to be on screen)
    - `.planning/phases/02-android-compression-on-media3/02-UAT.md` (item 6's exact wording and what would actually close it: progress advancing, the result line showing bytes, dimensions and elapsed, and cancel returning the button to idle)
    - `example/lib/src/main_screen.dart` (the screen being driven, so the walkthrough exercises every control rather than only the happy path)
    - `tool/mac_run.sh` (the simulator boot and UDID resolution this task reuses to install and launch the app)
    - `.planning/phases/03-apple-compression-to-parity/03-CONTEXT.md` → the specifics section (the screenshot mechanism and the documented fallback if macOS screen capture needs a permission an agent cannot grant)
  </read_first>
  <files>.planning/phases/03-apple-compression-to-parity/ios-simulator-compressing.png, .planning/phases/03-apple-compression-to-parity/ios-simulator-result.png, .planning/phases/03-apple-compression-to-parity/macos-result.png, .planning/phases/02-android-compression-on-media3/02-UAT.md, QUESTIONS.md</files>
  <action>
    Build and launch the example app on the booted iOS simulator, then drive the full flow with the bundled sample clip: confirm the empty state, start a compression, capture a screenshot while the progress bar is advancing, let it finish, confirm the result card shows the savings figure, the byte counts, the dimensions, the codec, the badges and the elapsed time, capture a second screenshot, and play the output. Then start another compression and cancel it, confirming the cancelled snackbar appears and the controls return to their enabled state. Use the simulator's own screenshot facility.
    Repeat the flow on the macOS host, where the desktop layout applies, and capture the done state. If capturing the macOS screen needs a permission this agent cannot grant, do not loop on it: record the exact failure, add a QUESTIONS.md item describing what Dan would need to allow, and fall back to the iOS screenshots plus a written account of what the macOS run showed — stating clearly in the summary which visual evidence is an image and which is a written observation.
    Commit the screenshots into this phase directory with descriptive filenames, and update 02-UAT.md item 6 to resolved, referencing the committed files by path and naming the platforms they came from. Leave the other five UAT items alone — they need a physical phone or a fixture this phase does not add, and marking them on anything less would be false.
    Read each committed screenshot back and describe what is actually visible in the summary, so the phase record contains a human-readable account and not only a file nobody looked at.
  </action>
  <acceptance_criteria>
    - At least two PNG files exist in `.planning/phases/03-apple-compression-to-parity/` and each is a non-empty, readable image.
    - The summary describes what is visible in each committed screenshot, including the progress percentage shown in the compressing shot and at least three fields from the result card in the done shot.
    - `grep -c '03-apple-compression-to-parity' .planning/phases/02-android-compression-on-media3/02-UAT.md` is at least 1, and item 6's result line is no longer `[pending]`.
    - Items 1 through 5 of 02-UAT.md are unchanged in `git diff`.
    - The cancel path was exercised on the simulator and its observed behaviour — the snackbar text and the control re-enablement — is recorded in the summary.
    - If macOS screen capture failed, a QUESTIONS.md item exists naming the permission needed, and the summary says plainly which evidence is visual and which is written.
  </acceptance_criteria>
  <verify>
    <automated>test "$(ls .planning/phases/03-apple-compression-to-parity/*.png 2>/dev/null | wc -l)" -ge 2 && test "$(grep -c '\[pending\]' .planning/phases/02-android-compression-on-media3/02-UAT.md)" -le 5</automated>
  </verify>
  <done>Someone has watched this package compress a video on an Apple device through the example app, and the evidence is committed where the next reader can see it rather than take it on trust.</done>
</task>

<task type="auto">
  <name>Task 2: One green run of the real pipeline, and a final three-platform sweep</name>
  <read_first>
    - `.github/workflows/ci.yml` (the `apple` and `parity` jobs as 03-08 left them, so the observed run is checked against what the workflow actually does)
    - `.planning/phases/03-apple-compression-to-parity/03-08-SUMMARY.md` (the run ids and wall-clock 03-08 recorded, so this task can tell a new failure from a known flake)
    - `.claude/CLAUDE.md` (the lane notes on CI path gating, on the emulator segfault seen once under memory pressure, and on the rule that a refused or flaky CI run is an external blocker rather than a task failure)
    - `.planning/phases/01-typed-contract-ci-and-media-info/01-07-PLAN.md` (how the previous phase's sign-off plan recorded an observed run and what it treated as sufficient)
  </read_first>
  <files>.planning/phases/03-apple-compression-to-parity/03-VALIDATION.md</files>
  <action>
    Push the phase's accumulated work and observe a full CI run. Both the `apple` job and the `parity` job must conclude successfully on one run; record its id and the conclusion of every job in the summary. If a job fails for a real reason, fix the cause and re-run. If it fails for a documented environmental reason — a hosted simulator hang that passes on retry, a runner disk or memory condition — say so explicitly with the evidence rather than counting it as green, and retry. Three failures of the same kind with no progress is the point to stop, record what was tried, and carry it forward as a blocker rather than looping.
    Independently of CI, run one final local sweep: every integration suite on the Android emulator, every integration suite on the iOS simulator, and every integration suite on the macOS host. Record the passing case count per platform per suite. A count that differs between platforms without an explanation is itself a finding — it means a case is skipping somewhere — and must be investigated rather than averaged away.
    Run the repository's own quality gates once more and record each result: the analyzer with infos and warnings fatal, the root Dart tests, the plugin's native Android unit tests, the XCTest suites on both Apple platforms, the corpus verifier, the parity gate's self-test, and the package publish dry run.
    Update the Status column of `03-VALIDATION.md`'s Per-Task Verification Map from the results of this sweep, so the phase's validation contract records what actually ran rather than what was planned to run.
  </action>
  <acceptance_criteria>
    - `gh run view <id> --json jobs --jq '.jobs[] | "\(.name) \(.conclusion)"'` shows both the Apple job and the cross-platform parity job with conclusion `success` for one run id, and that id is recorded in the summary.
    - `cd example && flutter test integration_test -d emulator-5554` exits 0, with the passing case count recorded.
    - `bash tool/mac_run.sh ios integration_test` exits 0, with the case count recorded.
    - `bash tool/mac_run.sh macos` exits 0, with the case count recorded.
    - Any per-platform case-count difference is explained in the summary, naming the case and the reason.
    - `flutter analyze --fatal-infos --fatal-warnings`, `flutter test`, `bash corpus/verify_corpus.sh`, `bash tool/check_parity_test.sh` and `dart pub publish --dry-run` all exit 0.
    - `cd example/android && ./gradlew :compress_video:testDebugUnitTest` exits 0, and `bash tool/mac_run.sh xctest-ios` and `bash tool/mac_run.sh xctest-macos` both exit 0.
    - `grep -c '⬜ pending' .planning/phases/03-apple-compression-to-parity/03-VALIDATION.md` is 0 — every row carries an observed status.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && flutter test && bash corpus/verify_corpus.sh && bash tool/check_parity_test.sh && dart pub publish --dry-run && bash tool/mac_sync.sh && bash tool/mac_run.sh ios integration_test && bash tool/mac_run.sh macos && cd example && flutter test integration_test -d emulator-5554</automated>
  </verify>
  <done>One real CI run shows the Apple and parity jobs green together, every suite passes on all three platforms locally with its case count recorded, and the phase's validation map reflects observed results rather than intentions.</done>
</task>

<task type="auto">
  <name>Task 3: Say exactly what "the same on every platform" means, and close the requirements honestly</name>
  <read_first>
    - `README.md` (the existing platform-support and behaviour wording this task extends, and where a parity statement belongs relative to it)
    - `lib/src/compress_result.dart` (the class dartdoc and the per-field dartdoc, especially `outputBytes` and `elapsedMs`, which are the two fields that are deliberately not cross-platform)
    - `doc/PRESETS.md` (the measured Android and Apple tables this wording points at as its substantiation)
    - `.planning/REQUIREMENTS.md` (the four rows this phase owns and the traceability table at the bottom, both hand-edited only)
    - `.planning/phases/02-android-compression-on-media3/02-07-SUMMARY.md` (how the previous phase's closing plan recorded which requirements it checked and what it carried forward unresolved)
    - `CHANGELOG.md` and `doc/TOOLCHAIN.md` (the existing entry style, and the toolchain pins that now include the Mac's second Flutter SDK and the Apple build paths)
  </read_first>
  <files>README.md, lib/src/compress_result.dart, CHANGELOG.md, doc/TOOLCHAIN.md, .claude/CLAUDE.md, .planning/REQUIREMENTS.md</files>
  <action>
    Add a short, plain section to the README stating what cross-platform sameness means in this package: the displayed dimensions, the video and audio codecs, the transmuxed, usedOriginal and audioReencoded flags and the duration are the same on Android, iOS and macOS within the documented per-clip tolerance, and the output byte count and elapsed time are deliberately not, because the platforms use different encoders. Point at `doc/PRESETS.md`'s per-platform measured tables as the substantiation. Mirror the same statement, more briefly, in the `CompressResult` class dartdoc and in the `outputBytes` and `elapsedMs` field dartdoc, since that is where a consumer reads it at the moment they would otherwise assume parity.
    Add the phase's entry to `CHANGELOG.md` in the existing style: the Apple engine, trim on all platforms, both install paths, and the example app. Update `doc/TOOLCHAIN.md` with the Apple-side toolchain facts this phase established — the Mac's second Flutter SDK path and version, the Xcode version, the iOS simulator runtime used, and which install path is proven where. Update `.claude/CLAUDE.md`'s lane notes with anything a future agent would otherwise re-derive: the Mac workflow scripts, the SPM-only local constraint, and any Apple-versus-Android tolerance divergence recorded during the phase.
    Then close the requirements, by hand, one row at a time, in `REQUIREMENTS.md` — never through a `gsd-tools` writer, which this project has recorded as corrupting planning markdown it does not even mention. For each of CORE-01, CORE-07, BULD-02 and BULD-04, check the row only if its evidence exists, and name that evidence in the summary: the observed CI run id, the parity job's result, the committed screenshots, the case counts. Update the traceability table's status for the same four rows. Review `git diff .planning/REQUIREMENTS.md` before committing and confirm it contains only those lines.
    Finally, write the carried-forward list into the summary: the unresolved BULD-02 minimum-deployment-target question 03-08 flagged, any audio or error branch still without a fixture, any tolerance that differs between Apple and Android and why, the five remaining 02-UAT items and what each needs, and anything else this phase could not prove. A phase that carries nothing forward after this much platform work is a phase that stopped looking.
  </action>
  <acceptance_criteria>
    - `grep -ci 'elapsed' README.md` is at least 1 in the new parity section, and the section names dimensions, codec, flags and duration as the guaranteed set.
    - `grep -c 'PRESETS' README.md` is at least 1.
    - The `CompressResult` class dartdoc and the `outputBytes` and `elapsedMs` field dartdoc each state the platform-dependence: `grep -c 'platform' lib/src/compress_result.dart` is at least 3.
    - `CHANGELOG.md` has a new entry naming the Apple engine, trim and both install paths.
    - `doc/TOOLCHAIN.md` names the Mac's second Flutter SDK path, its version and the Xcode version used.
    - Exactly four requirement rows change in `git diff .planning/REQUIREMENTS.md`, plus their four traceability rows, and nothing else.
    - Each of CORE-01, CORE-07, BULD-02 and BULD-04 is marked complete in `REQUIREMENTS.md` and its evidence is named in the summary.
    - `flutter analyze --fatal-infos --fatal-warnings` and `dart pub publish --dry-run` both exit 0 after the documentation changes.
    - The summary contains a carried-forward list with at least the BULD-02 minimum-deployment-target item and the five open 02-UAT items.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && dart pub publish --dry-run && grep -q 'PRESETS' README.md && test -n "$(git diff --name-only .planning/REQUIREMENTS.md)"</automated>
  </verify>
  <done>The package says exactly what it guarantees across platforms and what it does not, the four requirements this phase owns are checked against named evidence, and everything still open is written down where the next phase will find it.</done>
</task>

</tasks>

## Artifacts this phase produces

See `03-01-PLAN.md` → "Artifacts this phase produces" for the full phase-level list. This plan
contributes: the committed iOS simulator and macOS screenshots, the resolved 02-UAT.md item 6, the
cross-platform sameness wording in `README.md` and the `CompressResult` dartdoc, the phase's
`CHANGELOG.md` entry, the Apple toolchain facts in `doc/TOOLCHAIN.md`, the updated
`.claude/CLAUDE.md` lane notes, the observed-status Per-Task Verification Map in `03-VALIDATION.md`,
and the four hand-edited requirement rows in `REQUIREMENTS.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| observed CI result → project record | A run id recorded in the project's own history is what a future reader trusts instead of re-running everything |
| planning markdown → automated writer | `STATE.md`, `ROADMAP.md` and `REQUIREMENTS.md` are edited in this plan, and this project has a recorded data-loss bug in the tooling that would otherwise edit them |
| committed screenshot → phase evidence | An image committed into the planning directory is treated as proof by everyone downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-39 | Repudiation | A requirement marked complete with no traceable evidence, so a later reader cannot tell what proved it | high | mitigate | Each of the four rows is checked only with its evidence named in the summary — a run id, an artifact path or a recorded case count — and the `REQUIREMENTS.md` diff is reviewed to contain only those lines |
| T-03-40 | Tampering | An automated planning-file writer silently corrupting `REQUIREMENTS.md` or `STATE.md` values it never mentions | high | mitigate | Every planning-markdown edit in this plan is by hand, and the `git diff` of each file is reviewed before commit; this is the project's own recorded mitigation for the known tooling bug |
| T-03-41 | Spoofing | A green-looking CI result that came from a different commit, a different workflow or a partial run | high | mitigate | The run id is recorded and its per-job conclusions are read back from the API rather than from a badge or a memory of a passing run |
| T-03-42 | Information Disclosure | A committed screenshot exposing a filesystem path, a device identifier or unrelated on-screen content | medium | mitigate | The app renders only a basename and byte counts, the screenshots are read back and described before commit, and anything unexpected in frame is re-captured rather than committed |
| T-03-43 | Repudiation | A phase that carries nothing forward, hiding what it could not prove | medium | mitigate | The carried-forward list is a required part of the summary and names, at minimum, the BULD-02 floor-version question and the five remaining UAT items |
| T-03-SC | Tampering | Third-party package supply chain | low | accept | This plan adds no package from any registry; it changes documentation, planning records and committed images only |
</threat_model>

<verification>
1. At least two committed, readable screenshots exist in the phase directory, described in the summary, and 02-UAT.md item 6 references them and is no longer pending.
2. One recorded CI run id has both the Apple and the cross-platform parity jobs concluding successfully.
3. Every integration suite passes on the Android emulator, the iOS simulator and the macOS host, with case counts recorded and any difference explained.
4. The analyzer, the root Dart tests, both native unit suites, the corpus verifier, the parity self-test and the publish dry run all exit 0.
5. The README and the `CompressResult` dartdoc state what is and is not the same across platforms, pointing at the measured tables.
6. `REQUIREMENTS.md` shows exactly the four requirement rows and their four traceability rows changed, each with named evidence, hand-edited.
</verification>

<success_criteria>
- The phase's claim is backed by one observed green run of the real pipeline plus committed visual evidence of the app working on Apple, not by an agent's local say-so.
- The package documents precisely what it guarantees across platforms and what it deliberately does not.
- The four requirements this phase owns are closed against named evidence, and everything still open is written down.
</success_criteria>

<output>
Create `.planning/phases/03-apple-compression-to-parity/03-09-SUMMARY.md` when done.
</output>
