---
phase: 03-apple-compression-to-parity
plan: 08
type: execute
wave: 7
depends_on: ["03-07", "03-03"]
files_modified:
  - tool/verify_fresh_app.sh
  - tool/check_parity.sh
  - tool/check_parity_test.sh
  - .github/workflows/ci.yml
  - example/integration_test/compress_test.dart
  - example/integration_test/compress_audio_test.dart
  - example/integration_test/compress_jobs_test.dart
  - example/integration_test/compress_output_test.dart
autonomous: true
requirements: [BULD-02, CORE-01, CORE-07]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`tool/verify_fresh_app.sh` creates a throwaway Flutter app in a temporary directory, adds this plugin by path, and builds it for the iOS simulator and for macOS under CocoaPods and again under Swift Package Manager — four builds, one script, runnable on a CI runner and, for its SPM half, on the Mac where CocoaPods is absent (D-21, BULD-02)."
    - "The script cleans up its temporary app whether it succeeds or fails, and it never writes inside this repository's working tree."
    - "CI's `apple` job runs every `integration_test/*.dart` suite on the iOS simulator — the two-suite allowlist and its 'Phase 2 suites hang on iOS' comment are gone — keeping the per-suite perl `alarm` bound and the one simulator-reset retry (D-22)."
    - "CI's `apple` job also runs the integration suites on the macOS host as a desktop target, so the shared Swift core is exercised on both Apple platforms rather than merely compiled on one (D-22)."
    - "CI builds the example for iOS and macOS under CocoaPods and under Swift Package Manager, and runs `tool/verify_fresh_app.sh`, so BULD-02's 'installs through both' claim is a green build rather than an assertion (D-20, D-21)."
    - "The four compression suites emit `PARITY_JSON` lines carrying the deterministic `CompressResult` fields — displayed width and height, video codec, audio codec, transmuxed, usedOriginal, audioReencoded, and duration with the clip's own sidecar tolerance — for every case whose value is genuinely cross-platform (D-16)."
    - "`tool/check_parity.sh` compares those records with the same sidecar-driven tolerance rules it already applies to the media-info and thumbnail records, treats output bytes and elapsed milliseconds as tolerant rather than exact with a documented ±50% envelope on bytes so a tenfold regression still fails, and names the differing field on a mismatch (D-16)."
    - "`tool/check_parity_test.sh` grows fixture cases for the new compression fields proving both directions: a within-tolerance fixture passes and an out-of-tolerance one fails naming the field — the gate is proven before it is trusted."
    - "The `parity` job compares Android against iOS and macOS against iOS in the same step, because macOS and iOS run one shared Swift core and any delta between them is a real bug rather than a platform difference (D-22)."
  prohibitions:
    - statement: "MUST NOT compare output bytes or elapsed time for byte-exact equality across platforms — the encoders differ by design, and a strict comparison there would make the gate permanently red for a non-bug."
      status: active
      verification: "`grep -n 'outputBytes' tool/check_parity.sh` shows it handled in the tolerant branch with the documented envelope, and the self-test includes a passing case where the two platforms' byte counts differ within it."
    - statement: "MUST NOT let the `parity` job silently skip — an `apple` job that did not run must produce either an explicitly justified no-op or a loud failure, never a green-by-absence."
      status: active
      verification: "The existing gate step at the top of the `parity` job is preserved intact in the diff and its fatal branches are unchanged."
    - statement: "MUST NOT widen a parity tolerance to make a real cross-platform delta pass — a genuine divergence is documented in `corpus/README.md` with its measured values and, if it is intended, encoded as a named per-field rule rather than a blanket loosening."
      status: active
      verification: "Any tolerance added in this plan names the field it applies to and cites the measurement that justified it, in both the script comment and the summary."
    - statement: "MUST NOT run `tool/verify_fresh_app.sh` against Dan's `~/flutter` on the Mac, and MUST NOT let it modify this repository."
      status: active
      verification: "The script resolves Flutter from `PATH` only, `mac_run.sh` puts `~/development/flutter-stable/bin` first, and `git status --porcelain` is clean immediately after a local run."
  artifacts:
    - path: "tool/verify_fresh_app.sh"
      provides: "BULD-02's four-build proof as a script both CI and the Mac can run"
      contains: "flutter create"
    - path: ".github/workflows/ci.yml"
      provides: "An apple job that runs every suite on the simulator and on macOS, both install paths, and the fresh-app proof"
      contains: "integration_test"
    - path: "tool/check_parity.sh"
      provides: "Cross-platform comparison extended from media info and thumbnails to compression results"
      contains: "transmuxed"
  key_links:
    - from: "example/integration_test/compress_test.dart"
      to: "tool/check_parity.sh"
      via: "the PARITY_JSON lines CI greps out of each runner's log, which is the only thing that turns 'the same test ran on both' into 'both observed the same values'"
      pattern: "PARITY_JSON"
    - from: ".github/workflows/ci.yml"
      to: "tool/verify_fresh_app.sh"
      via: "the apple job step that makes BULD-02 a build result rather than a claim"
      pattern: "verify_fresh_app.sh"
---

<objective>
Turn every Apple guarantee this phase has proven by hand into something a push proves
automatically, and widen the parity gate from media info to compression.

Purpose: the phase's headline claim is that the same call gives the same observable result on
three platforms. Until CI runs all four compression suites on the simulator AND on macOS, and
until the parity gate diffs the values each runner actually observed, that claim rests on an
agent having run things locally. BULD-02 has the same shape: "installs through CocoaPods and SPM"
is only credible as four green builds of a fresh app that depends on nothing but the published
plugin directory.
Output: the fresh-app proof script, a widened `apple` job, compression `PARITY_JSON` records, and
a parity gate that compares Android against iOS and macOS against iOS with its own self-test
extended to match.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-apple-compression-to-parity/03-CONTEXT.md
@.planning/phases/03-apple-compression-to-parity/03-01-PLAN.md
@.planning/phases/03-apple-compression-to-parity/03-07-PLAN.md
@corpus/README.md
</context>

## Flagged assumption carried into this plan

The deterministic edge probe returned one `unclassified` row for **BULD-02** that it could not
categorise, and it is recorded here rather than dropped or invented into a criterion. What
"iOS 13+ and macOS 11+ share one Swift core and the package installs through both CocoaPods and
SPM" means at its edges — specifically whether the four fresh-app builds must also be proven
against the *minimum* deployment targets rather than only against the runner's current SDK — is
not resolved by any source artifact in this phase. This plan proves the four builds on the current
toolchain, which is what D-20 and D-21 specify; a minimum-deployment-target build matrix is
neither planned nor claimed. The verifier should treat "BULD-02 proven at the floor OS versions"
as **unresolved**, not as satisfied.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a fresh app adds this plugin and builds" — four builds, one script</name>
  <precondition>`bash tool/mac_sync.sh && bash tool/mac_run.sh build-macos` exits 0, confirming the Mac toolchain from 03-01 still works before a script that drives four builds is written against it.</precondition>
  <read_first>
    - `tool/verify_apk_native_libs.sh` (the project's shell conventions and, more importantly, the shape of a proof script: enumerate, assert, fail loudly and name what failed)
    - `.github/workflows/ci.yml` lines 279-441 (the `apple` job's existing CocoaPods-versus-SPM handling, including the explicit `flutter config --no-enable-swift-package-manager` step and the documented reason it exists, and the Pods-state-clearing retry — the fresh app hits the same integration behaviour)
    - `pubspec.yaml` and `.pubignore` (what a consumer actually gets when they depend on this package, which is what the fresh app must depend on)
    - `darwin/compress_video.podspec` and `darwin/compress_video/Package.swift` (the two manifests whose parity this script is proving)
    - `.planning/phases/03-apple-compression-to-parity/03-RESEARCH.md` → "Environment Availability" (CocoaPods is absent on the Mac, so only the SPM half runs locally and CI proves the other)
  </read_first>
  <files>tool/verify_fresh_app.sh</files>
  <action>
    Write `tool/verify_fresh_app.sh` following the project's shell conventions. It creates a throwaway Flutter app in a temporary directory outside this repository, adds this plugin as a path dependency pointing at the repository root, and then performs four builds: an iOS simulator build without code signing and a debug macOS build, once with Swift Package Manager disabled so CocoaPods integration is genuinely exercised, and once with it enabled. Each build prints a labelled banner so a CI log shows which of the four failed. Clean up the temporary directory on every exit path including failure, and restore whatever Flutter SPM configuration the environment had before the script ran, since that setting is global to the SDK.
    Give the script a way to run only the SPM half — an argument or an environment variable — because the Mac has no CocoaPods and attempting that half there would fail for an environmental reason rather than a real one. Document that in the header alongside the reason.
    Prove it end to end now: run the SPM half on the Mac through `tool/mac_run.sh`, and confirm both SPM builds pass. The CocoaPods half is proven by CI in task 2; the summary must state clearly which half was proven where.
  </action>
  <acceptance_criteria>
    - `bash -n tool/verify_fresh_app.sh` exits 0 and the file is executable.
    - `bash tool/mac_run.sh shell 'bash tool/verify_fresh_app.sh --spm-only'` exits 0, with both SPM builds succeeding and a labelled banner per build in the output.
    - The script leaves nothing behind: `ssh dans-macbook-air 'ls /tmp | grep -c compress_video_fresh'` is 0 after the run, or the equivalent for whatever temporary prefix the script uses.
    - `git status --porcelain` is clean immediately after the run, proving the script did not write inside the repository.
    - `grep -c 'flutter create' tool/verify_fresh_app.sh` is at least 1 and `grep -c 'no-enable-swift-package-manager' tool/verify_fresh_app.sh` is at least 1.
    - The script restores the previous SPM setting: `grep -c 'enable-swift-package-manager' tool/verify_fresh_app.sh` is at least 2 (one to set, one to restore).
  </acceptance_criteria>
  <verify>
    <automated>bash -n tool/verify_fresh_app.sh && bash tool/mac_sync.sh && bash tool/mac_run.sh shell 'bash tool/verify_fresh_app.sh --spm-only' && git status --porcelain</automated>
  </verify>
  <done>A fresh Flutter app that knows nothing about this repository except its path depends on the plugin and builds for iOS and macOS through Swift Package Manager, proven on the Mac, with the CocoaPods half ready for CI.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the CI apple job to every suite, both Apple platforms and both install paths</name>
  <read_first>
    - `.github/workflows/ci.yml` (read the `changes` job at lines 29-77, the `apple` job at 279-440 and the `parity` job at 441-504 in full — every step this task edits, plus the path filter that decides whether the job runs at all)
    - `.claude/CLAUDE.md` (the lane note about the `apple` job being path-gated to protect runner minutes, and the note about `reactivecircus` script lines being separate shells — the latter applies to the Android job, not these macOS steps, but the perl `alarm` convention does apply here)
    - `.planning/phases/01-typed-contract-ci-and-media-info/01-CONTEXT.md` (the original CI design, including the dedicated SPM build step it called for, so this task adds only what 01-06 did not already land)
    - `.planning/phases/03-apple-compression-to-parity/03-CONTEXT.md` → the CI decision (D-22's exact scope: drop the allowlist, keep the alarm and retry, add the macOS desktop run)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Replace the `apple` job's explicit two-suite list with a loop over every `integration_test/*.dart` file, and delete the stale comment above it claiming the compression suites are restricted to one platform until this phase lands — it is no longer true, and a stale comment here would mislead the next person into narrowing the list again. Keep the per-suite perl `alarm` bound and the one simulator-reset retry exactly as they are, and raise the step's own timeout if the wider suite list needs it, stating the new budget's basis.
    Add a macOS desktop integration run of the same directory as a separate step, capturing its log the same way the iOS run does so its `PARITY_JSON` lines can be extracted.
    Extract the parity records into three artifacts rather than two: Android, iOS and macOS, so the parity job can compare both pairs. Keep the existing `tee`-plus-`grep` capture approach and the `set -o pipefail` that makes a failing suite fail the step.
    Add a macOS build under Swift Package Manager beside the existing SPM iOS build, so both install paths cover both Apple platforms, and add a step running `tool/verify_fresh_app.sh` in full — both halves — since the runner has CocoaPods.
    Leave the path filter alone: the `apple` job stays gated on Apple-relevant paths so a Markdown-only push still creates no macOS runner time, which the lane notes record as a deliberate courtesy.
    Push and observe a real run. A red run that reveals a genuine problem is the point of this task; iterate until the `apple` job concludes successfully, and record the run id and the wall-clock duration of the widened job in the summary so the next phase knows what it costs.
  </action>
  <acceptance_criteria>
    - The allowlist is gone, replaced by a directory loop: `grep -c 'integration_test/media_info_test.dart integration_test/thumbnail_test.dart' .github/workflows/ci.yml` is 0.
    - The stale restriction comment is gone: `grep -c 'Android-only until Phase 3' .github/workflows/ci.yml` is 0.
    - `grep -c 'd macos' .github/workflows/ci.yml` is at least 1, for the macOS desktop integration run.
    - `grep -c 'verify_fresh_app.sh' .github/workflows/ci.yml` is at least 1.
    - `grep -c 'parity-macos' .github/workflows/ci.yml` is at least 1, for the third artifact.
    - `grep -c 'alarm' .github/workflows/ci.yml` is at least as high as before this task, confirming the bound was kept rather than dropped.
    - A pushed CI run's `apple` job concludes `success`, and its run id is recorded in the summary: `gh run list --limit 5` shows it.
    - The `changes` path filter block is unchanged in `git diff`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'Android-only until Phase 3' .github/workflows/ci.yml)" = "0" && grep -q 'verify_fresh_app.sh' .github/workflows/ci.yml && grep -q 'parity-macos' .github/workflows/ci.yml && git push github HEAD:main && gh run list --limit 3</automated>
  </verify>
  <done>Every compression suite runs on the iOS simulator and on the macOS host on every Apple-relevant push, both install paths build both platforms, and a fresh app proves the package installs — all observed green on a real run.</done>
</task>

<task type="auto">
  <name>Task 3: Emit and compare compression parity records across Android, iOS and macOS</name>
  <read_first>
    - `tool/check_parity.sh` (read in full — the merge of `PARITY_JSON` lines, the key-set equality check, the sidecar-driven tolerance rules for duration and thumbnail RGB, and the mismatch reporting format this task extends)
    - `tool/check_parity_test.sh` (read in full — the hand-written fixture pattern and the both-directions proof convention every new field must follow)
    - `example/integration_test/media_info_test.dart` lines 130-160 and `thumbnail_test.dart` lines 90-110 (the existing `PARITY_JSON` emission shape the compression suites must match)
    - `example/integration_test/compress_test.dart` (the cases whose results are genuinely cross-platform and therefore worth recording, versus the ones whose values are encoder-dependent)
    - `corpus/README.md` (the `crossPlatform` versus `tolerant` field convention, and the small_480p duration finding 03-07 recorded, which decides how duration is compared here)
  </read_first>
  <files>example/integration_test/compress_test.dart, example/integration_test/compress_audio_test.dart, example/integration_test/compress_jobs_test.dart, example/integration_test/compress_output_test.dart, tool/check_parity.sh, tool/check_parity_test.sh</files>
  <action>
    Add `PARITY_JSON` emission to the four compression suites following the existing shape: accumulate a record per named case and print one line at the end of the suite. Record only the deterministic fields — the displayed width and height, the video and audio codecs, and the transmuxed, usedOriginal and audioReencoded flags — plus the duration, which is compared against the clip's own sidecar tolerance rather than exactly. Record output bytes and elapsed milliseconds in a separate tolerant section, never as exact values, because the encoders genuinely differ.
    Extend `tool/check_parity.sh` to understand the new record keys: exact equality for the flags, codecs and dimensions; the existing sidecar duration tolerance for duration; and a documented envelope of plus or minus fifty percent for output bytes so a tenfold regression still fails while a legitimate encoder difference does not. Elapsed time is recorded for the log but not compared. Keep the existing behaviour that a key present on only one platform is itself a failure — a case that silently did not run on one platform is exactly what this gate exists to catch.
    Extend `tool/check_parity_test.sh` with fixture cases for every new field in both directions: a within-envelope byte difference passes, an out-of-envelope one fails naming the field, a flipped transmuxed flag fails, a differing codec fails, and a duration inside the sidecar tolerance passes. The gate must be proven before it is trusted, which is why its self-test runs first in the CI job.
    Update the `parity` job to run the comparison twice — Android against iOS, then macOS against iOS — reporting both. Note in a step comment that macOS and iOS share one Swift core, so a delta between them is a real bug rather than a platform difference, which is why they are compared at the same strictness as the cross-engine pair.
    Push and observe. If a real cross-platform delta surfaces, do not widen a tolerance to hide it: investigate, record it in `corpus/README.md` with its measured values as 01-07 did for the duration delta, and encode a named per-field rule only if the delta is genuinely intended.
  </action>
  <acceptance_criteria>
    - `bash tool/check_parity_test.sh` exits 0 and covers at least five new compression-field cases, each proving both directions.
    - `grep -c 'PARITY_JSON' example/integration_test/compress_test.dart` is at least 1, and the same for the other three compression suites.
    - `grep -c 'transmuxed' tool/check_parity.sh` is at least 1 and `grep -c 'outputBytes' tool/check_parity.sh` is at least 1.
    - A deliberately corrupted fixture proves the gate has teeth: flipping one flag in a self-test fixture makes `check_parity_test.sh` report the failure and name the field, demonstrated once and recorded in the summary.
    - The `parity` job runs two comparisons: `grep -c 'check_parity.sh' .github/workflows/ci.yml` is at least 2.
    - A pushed CI run's `parity` job concludes `success` with both comparisons green, and its run id is recorded in the summary.
    - `cd example && flutter test integration_test -d emulator-5554` still exits 0 with the new emission in place.
    - Any cross-platform delta discovered is documented in `corpus/README.md` with its measured values rather than absorbed into a widened tolerance.
  </acceptance_criteria>
  <verify>
    <automated>bash tool/check_parity_test.sh && cd example && flutter test integration_test -d emulator-5554 && cd .. && git push github HEAD:main && gh run list --limit 3</automated>
  </verify>
  <done>Each runner's own observed compression results are compared field by field against the other two, with a self-tested gate that names what differed and refuses to treat a missing record as a match.</done>
</task>

</tasks>

## Artifacts this phase produces

See `03-01-PLAN.md` → "Artifacts this phase produces" for the full phase-level list. This plan
contributes: `tool/verify_fresh_app.sh`, the widened `apple` job (directory-wide suite loop, macOS
desktop integration run, SPM macOS build, fresh-app step, third parity artifact), `PARITY_JSON`
emission in all four compression suites, the extended `tool/check_parity.sh` and
`tool/check_parity_test.sh`, and the two-comparison `parity` job.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository contents → CI runner | Every script and workflow step this plan adds executes on a hosted runner with the repository's own contents as input |
| test logs → parity gate | The gate's entire input is text scraped from two or three runner logs; a missing or empty artifact must never read as agreement |
| temporary fresh app → developer machine | `verify_fresh_app.sh` creates and deletes a directory outside the repository, on CI and on the Mac |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-34 | Tampering | A CI or verification script executing text it read from a file or a log rather than from its own source | high | mitigate | Neither `verify_fresh_app.sh` nor `check_parity.sh` evaluates any input: the former runs a fixed sequence of `flutter` invocations with no interpolated input, and the latter parses log text only as JSON through `jq`, never as shell |
| T-03-35 | Repudiation | A parity gate that passes because an artifact was missing or a suite silently did not run | high | mitigate | The existing behaviour is preserved and extended: a missing or empty parity file is fatal, a key present on only one platform is a failure, and the `parity` job's gate step turns a skipped `apple` job into either a justified no-op or a loud failure |
| T-03-36 | Tampering | A parity gate that cannot actually fail, giving false assurance | high | mitigate | `check_parity_test.sh` proves both directions for every new field, and this task additionally demonstrates the teeth once by corrupting a fixture and observing the named failure |
| T-03-37 | Denial of Service | The widened suite list exhausting the runner's time budget and turning a real failure into an unattributable timeout | medium | mitigate | The per-suite perl `alarm` bound and the one-retry pattern are preserved, the step timeout is raised with its basis stated, and the widened job's measured wall-clock is recorded in the summary |
| T-03-38 | Tampering | The fresh-app script leaving a modified global Flutter configuration or a stray temporary app behind | low | mitigate | The script restores the previous Swift Package Manager setting and removes its temporary directory on every exit path, asserted after a real run |
| T-03-SC | Tampering | Third-party package supply chain | low | accept | This plan adds no package from any registry; the throwaway app's dependencies are Flutter's own `create` template plus this plugin by path |
</threat_model>

<verification>
1. `bash tool/verify_fresh_app.sh --spm-only` passes on the Mac and the full four-build run passes on the CI runner.
2. The `apple` job runs every `integration_test/*.dart` suite on the simulator and on macOS, with the allowlist and its stale comment removed and the alarm bound kept.
3. Three parity artifacts are produced and the `parity` job runs two comparisons, both green, on an observed run whose id is recorded.
4. `bash tool/check_parity_test.sh` exits 0 with at least five new compression-field cases proving both directions, and the gate's teeth are demonstrated once.
5. The Android emulator suite still passes with the new `PARITY_JSON` emission in place.
6. The `changes` path filter is unchanged, so a documentation-only push still creates no macOS runner time.
</verification>

<success_criteria>
- A fresh Flutter app installs this plugin and builds for iOS and macOS under both CocoaPods and Swift Package Manager, as four green CI builds.
- Every compression suite runs on the iOS simulator and the macOS host on every Apple-relevant push.
- The parity gate compares what each runner actually observed across all three platforms, names what differs, and cannot pass by silence.
</success_criteria>

<output>
Create `.planning/phases/03-apple-compression-to-parity/03-08-SUMMARY.md` when done.
</output>
