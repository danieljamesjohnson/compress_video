---
phase: 01-typed-contract-ci-and-media-info
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/CLAUDE.md
  - doc/TOOLCHAIN.md
  - .planning/phases/01-typed-contract-ci-and-media-info/COVERAGE.md
  - .planning/phases/01-typed-contract-ci-and-media-info/01-VALIDATION.md
autonomous: true
requirements: [BULD-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`flutter doctor -v` on danserver reports an Android toolchain section with no missing-component error lines."
    - "A headless Android emulator boots on danserver and `adb devices` lists exactly one line ending in `device` (not `offline`, not `unauthorized`)."
    - "`gh repo view danieljamesjohnson/compress_video --json visibility,defaultBranchRef` reports `private` and default branch `main`, and its HEAD commit sha equals local `main`."
    - "`git remote -v` lists both `origin` (danserver bare repo) and `github` (github.com/danieljamesjohnson/compress_video); pushing to one does not change the other."
    - "The GitHub repository's default workflow token permission is `read`, so no workflow can write to the repository without an explicit per-job grant."
    - "`doc/TOOLCHAIN.md` records the installed version of Flutter, Dart, JDK, AGP, Kotlin, Gradle, compileSdk, minSdk, Pigeon, flutter_lints, media3 and both CI runner images, each with the date it was verified."
    - "`.planning/phases/01-typed-contract-ci-and-media-info/COVERAGE.md` exists and declares that this phase integrates no external API."
    - "`01-VALIDATION.md` names a real test framework, a real quick-run command, a real full-suite command, and one Per-Task Verification Map row for every task in plans 01-01 through 01-07."
  artifacts:
    - path: "doc/TOOLCHAIN.md"
      provides: "Quarterly toolchain-rot baseline: every pinned version with its verification date"
      contains: "Verified on"
    - path: ".planning/phases/01-typed-contract-ci-and-media-info/COVERAGE.md"
      provides: "API-coverage declaration for this phase"
      contains: "No external API integration"
    - path: ".planning/phases/01-typed-contract-ci-and-media-info/01-VALIDATION.md"
      provides: "Filled Nyquist validation contract for phase 1"
      contains: "Per-Task Verification Map"
    - path: ".claude/CLAUDE.md"
      provides: "Lane notes recording the exact Android SDK install so the next agent does not re-derive it"
      contains: "ANDROID_HOME"
  key_links:
    - from: "danserver shell environment"
      to: "~/Android/Sdk"
      via: "ANDROID_HOME / ANDROID_SDK_ROOT exported from ~/.profile and ~/.bashrc, plus `flutter config --android-sdk`"
      pattern: "ANDROID_(HOME|SDK_ROOT)"
    - from: "local git repository"
      to: "github.com/danieljamesjohnson/compress_video"
      via: "git remote named `github`, added alongside the existing danserver `origin`"
      pattern: "remote.github.url"
---

<objective>
Make danserver able to build and run Android code, and put the repository on GitHub so Actions can
run on it. Also write the three phase-level declaration documents that the rest of the phase is
measured against: the toolchain pin baseline, the API-coverage declaration, and the filled
validation contract.

Purpose: every other plan in this phase needs a working Android SDK plus emulator (nothing Android
can be verified without them) and a GitHub remote (the macOS CI runner is this phase's only Apple
verification path, because SSH to the MacBook Air is refused — QUESTIONS.md #1).
Output: a working `~/Android/Sdk`, a booting headless emulator, a private GitHub repository with a
`github` remote, `doc/TOOLCHAIN.md`, `COVERAGE.md`, and a filled `01-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-typed-contract-ci-and-media-info/01-CONTEXT.md
@.planning/phases/01-typed-contract-ci-and-media-info/01-RESEARCH.md
@.claude/CLAUDE.md
@QUESTIONS.md
</context>

## Environment facts verified 2026-09-15 (do not re-derive)

| Fact | Value |
|------|-------|
| Flutter / Dart | 3.44.1 stable / 3.12.1 at `/home/dan/development/flutter/bin` (prepend to PATH in every Bash step) |
| Java | **not installed** (`java` not on PATH) |
| Android SDK | **not installed** |
| `/dev/kvm` | present, group `kvm`; `dan` is in `adm cdrom sudo dip video plugdev users lpadmin docker render` — **not** `kvm` |
| `gh` | 2.93.0, authenticated as `danieljamesjohnson` (free plan) |
| git remotes | only `origin` = `git@danserver.tailc2efd2.ts.net:danserver/compress-video.git` |
| ffmpeg / ffprobe / python3 | 6.1.1-3ubuntu5 / 6.1.1-3ubuntu5 / 3.12.3 |
| sudo | passwordless |

## Validation strategy values (transcribe these into 01-VALIDATION.md)

**Test Infrastructure**

| Property | Value |
|----------|-------|
| Framework | Dart `flutter_test` (unit) + `integration_test` (emulator/simulator) + JUnit 5 with Mockito (Android native unit) + XCTest (Apple native unit) |
| Config file | `analysis_options.yaml` + `test/` + `example/integration_test/` + `android/src/test/kotlin/` + `example/{ios,macos}/RunnerTests/` — all created in plan 01-03 / 01-04 |
| Quick run command | `flutter test` |
| Full suite command | `.github/workflows/ci.yml` — the `android` job (Dart unit + Gradle unit + emulator integration) and the `apple` job (XCTest + iOS-simulator integration + macOS build) both green |
| Estimated runtime | quick ~20 s local; full ~25 min in CI |

**Sampling Rate**

- After every task commit: `flutter test`
- After every plan wave: the full CI workflow on the pushed ref, checked with `gh run watch`
- Before `/gsd-verify-work`: both CI jobs green on the phase's final commit
- Max feedback latency: 20 s locally (`flutter test`), ~25 min for the full suite

**Per-Task Verification Map** — one row per task, `Task ID` = `1-<plan>-<task>`:

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|---|---|---|---|---|
| 1-01-01 | 01 | 1 | BULD-05 | T-01-01 / T-01-02 | SDK archive checksum verified before unzip; sudo limited to apt + usermod | cli | `flutter doctor -v` + `adb devices` | ✅ | ⬜ pending |
| 1-01-02 | 01 | 1 | BULD-05 | T-01-03 | Repo created private; default workflow token read-only | cli | `gh repo view --json visibility` | ✅ | ⬜ pending |
| 1-01-03 | 01 | 1 | BULD-05 | — | N/A | cli | `grep -q 'Verified on' doc/TOOLCHAIN.md` | ✅ | ⬜ pending |
| 1-02-01 | 02 | 1 | INFO-01 | T-01-04 | ffmpeg runs only on locally generated input | cli | `bash corpus/generate_corpus.sh && ffprobe ...` | ✅ | ⬜ pending |
| 1-02-02 | 02 | 1 | INFO-01, INFO-02 | T-01-05 | Sidecars regenerated from the committed clips, never hand-edited | cli | `bash corpus/verify_corpus.sh` | ✅ | ⬜ pending |
| 1-03-01 | 03 | 2 | BULD-03 | T-01-06 | Dependency pins from the verified legitimacy audit | unit | `flutter test test/compress_video_exception_test.dart` | ✅ | ⬜ pending |
| 1-03-02 | 03 | 2 | BULD-03 | T-01-07 | `.pubignore` keeps planning + corpus out of the published package | cli | `dart pub publish --dry-run` | ✅ | ⬜ pending |
| 1-03-03 | 03 | 2 | BULD-05 | T-01-SC, T-01-17 | Actions pinned to major tags; `permissions: contents: read` | ci | `gh run view <id> --json conclusion` | ✅ | ⬜ pending |
| 1-04-01 | 04 | 3 | BULD-03 | T-01-06 | Typed contract replaces untyped channel maps | unit | `flutter test test/messages_contract_test.dart` | ✅ | ⬜ pending |
| 1-04-02 | 04 | 3 | INFO-01 | T-01-08, T-01-09 | Path validated; decoder failure mapped to a typed reason | integration | `cd example && flutter test integration_test/media_info_test.dart` | ✅ | ⬜ pending |
| 1-04-03 | 04 | 3 | INFO-01, BULD-05 | T-01-10 | No file path or metadata written to the platform log | unit+integration | `./gradlew :compress_video:testDebugUnitTest` + `flutter test integration_test` | ✅ | ⬜ pending |
| 1-05-01 | 05 | 4 | INFO-02 | T-01-11, T-01-13 | outputPath canonicalised; maxDimensionPx bounded | integration | `cd example && flutter test integration_test/thumbnail_test.dart` | ✅ | ⬜ pending |
| 1-05-02 | 05 | 4 | INFO-02 | T-01-12 | Thumbnail files land in the app-private cache directory | integration | `cd example && flutter test integration_test/thumbnail_test.dart` | ✅ | ⬜ pending |
| 1-05-03 | 05 | 4 | INFO-02 | T-01-11 | Argument validation unit-tested on pure Kotlin | unit | `./gradlew :compress_video:testDebugUnitTest` | ✅ | ⬜ pending |
| 1-06-01 | 06 | 4 | INFO-01 | T-01-14, T-01-15 | No force-unwrap on AVFoundation optionals | unit | `xcodebuild test` (apple CI job) | ✅ | ⬜ pending |
| 1-06-02 | 06 | 4 | INFO-02 | T-01-14, T-01-16 | Thumbnail writes confined to the caches directory | unit | `xcodebuild test` (apple CI job) | ✅ | ⬜ pending |
| 1-06-03 | 06 | 4 | INFO-01, INFO-02 | T-01-15 | Pure-logic parity with Android asserted by XCTest | unit | `xcodebuild test` (apple CI job) | ✅ | ⬜ pending |
| 1-07-01 | 07 | 5 | BULD-05 | T-01-18 | Third-party actions pinned to reviewed major tags | ci | `gh run view <id> --json conclusion,jobs` | ✅ | ⬜ pending |
| 1-07-02 | 07 | 5 | INFO-01, INFO-02 | T-01-19 | CI logs carry no absolute user paths or secrets | ci | `gh run view <id> --log` + `gh run view --json conclusion` | ✅ | ⬜ pending |
| 1-07-03 | 07 | 5 | BULD-03, BULD-05 | — | N/A | cli | `grep -c 'nyquist_compliant: true' 01-VALIDATION.md` | ✅ | ⬜ pending |

**Wave 0 Requirements** — all test infrastructure is created inside this phase: `test/` (plan 01-03 T1),
`example/integration_test/media_info_test.dart` (01-04 T2), `example/integration_test/thumbnail_test.dart`
(01-05 T1), `android/src/test/kotlin/**` (01-04 T3), `example/{ios,macos}/RunnerTests/**` (01-03 T2, replaced 01-06 T3),
`.github/workflows/ci.yml` (01-03 T3).

**Manual-Only Verifications** — one: HEVC hardware encode and HDR tone-map fidelity on a physical
phone are out of this phase (Phase 4, QUESTIONS.md #3). All phase-1 behaviours have automated verification.

<tasks>

<task type="auto">
  <name>Task 1: Install the Android toolchain and boot a headless emulator on danserver</name>
  <precondition>`/dev/kvm` exists on danserver (`ls -l /dev/kvm` succeeds) and `sudo -n true` succeeds.</precondition>
  <reversibility rating="reversible">The SDK lives entirely under `~/Android/Sdk`; removing that directory and the two exported variables undoes it.</reversibility>
  <read_first>
    - `.claude/CLAUDE.md` (lane notes section — you will append to it)
    - `.planning/phases/01-typed-contract-ci-and-media-info/01-CONTEXT.md` (the "Local Android toolchain on danserver" bullet: exact package list and emulator flags)
    - `.planning/phases/01-typed-contract-ci-and-media-info/01-RESEARCH.md` ("Environment Availability" table and the AGP 9.0.1 / JDK 17 note)
  </read_first>
  <files>.claude/CLAUDE.md</files>
  <action>
    Install `openjdk-17-jdk-headless` with `sudo apt-get install -y` (AGP 9.0.1 with Gradle 9 requires JDK 17 minimum).
    Fetch the current Linux "Command line tools only" archive URL by curling `https://developer.android.com/studio` and extracting the href matching `commandlinetools-linux-*_latest.zip` — do not hard-code a build number. Download it, record its `sha256sum` in the commit message, and unzip it so the tools land at `~/Android/Sdk/cmdline-tools/latest/bin/sdkmanager` (the archive unpacks to a bare `cmdline-tools/` directory; it must be renamed to `latest` and nested, or sdkmanager refuses to run).
    Accept licences with `yes | sdkmanager --licenses`, then install exactly: `platform-tools`, `platforms;android-36`, `emulator`, the highest `build-tools;36.x.x` that `sdkmanager --list` offers, and `system-images;android-35;google_apis;x86_64`. If `sdkmanager --list` does not offer the android-35 system image, fall back to `system-images;android-36;google_apis;x86_64` and record which one you used.
    Export `ANDROID_HOME="$HOME/Android/Sdk"`, `ANDROID_SDK_ROOT="$HOME/Android/Sdk"` and add `$ANDROID_HOME/platform-tools`, `$ANDROID_HOME/cmdline-tools/latest/bin` and `$ANDROID_HOME/emulator` to PATH from BOTH `~/.profile` and `~/.bashrc` (append, do not rewrite either file). Run `flutter config --android-sdk "$HOME/Android/Sdk"`.
    Add `dan` to the `kvm` group with `sudo usermod -aG kvm dan`. Group membership does not apply to the current session, so run every emulator/adb command in this session through `sg kvm -c '<cmd>'`, exactly as the global CLAUDE.md prescribes for `sg docker`. Do not `chmod 666 /dev/kvm`.
    Create the AVD with `avdmanager create avd -n compress_video_api35 -k "<the system image you installed>" -d pixel_6 --force`, then boot it headless with the flags `-no-window -gpu swiftshader_indirect -no-audio -no-boot-anim -no-snapshot` in the background, and wait for readiness with `adb wait-for-device` followed by polling `adb shell getprop sys.boot_completed` until it returns `1` (cap the poll at 300 s and fail loudly on timeout — do not swallow the timeout).
    Append to the "Lane notes (danserver)" section of `.claude/CLAUDE.md` (the hand-written block after the `GSD:profile-end` marker, which `gsd-update` leaves alone): the SDK root, the exact sdkmanager package ids installed, the AVD name, the headless boot command line, the `sg kvm -c` requirement until Dan's next full re-login, and `flutter config --android-sdk` having been run. Keep the existing bullet that says the SDK is not installed only if you rewrite it to say it now is, with the install date.
  </action>
  <acceptance_criteria>
    - `java -version` prints a `17.` version string.
    - `sdkmanager --list_installed` output contains `platform-tools`, `platforms;android-36`, `emulator`, a `build-tools;36.` entry, and a `system-images;android-3` entry.
    - `flutter doctor -v` exits 0 and its output contains no line matching `✗` within the Android toolchain section.
    - `sg kvm -c 'adb devices'` prints exactly one device line whose second column is `device`.
    - `sg kvm -c 'adb shell getprop sys.boot_completed'` prints `1`.
    - `bash -lc 'echo $ANDROID_HOME'` prints `/home/dan/Android/Sdk`.
    - `grep -c 'ANDROID_HOME' .claude/CLAUDE.md` is at least 1.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'flutter doctor -v' && sg kvm -c 'adb devices' | grep -E '^emulator-[0-9]+\s+device$'</automated>
  </verify>
  <done>JDK 17 and the Android SDK are installed under `~/Android/Sdk`, a headless emulator is booted and reachable via adb, and `.claude/CLAUDE.md` records every step.</done>
</task>

<task type="auto">
  <name>Task 2: Create the private GitHub repository and wire it as a second remote</name>
  <precondition>`gh auth status` reports an authenticated account `danieljamesjohnson`.</precondition>
  <reversibility rating="costly">The repository name `compress_video` under `danieljamesjohnson` becomes the CI identity and, once Actions history and any external reference exist, renaming means re-pointing remotes and re-running history; visibility is Dan's to flip later (QUESTIONS.md #5). Both values are already fixed by CONTEXT.md, so no gate is inserted.</reversibility>
  <read_first>
    - `QUESTIONS.md` (item 5 — visibility is Dan's decision; the plan creates it private)
    - `.planning/phases/01-typed-contract-ci-and-media-info/01-CONTEXT.md` (the "CI design" first bullet)
  </read_first>
  <files>(no repository files — git remote configuration and GitHub-side state only)</files>
  <action>
    Create the repository with `gh repo create danieljamesjohnson/compress_video --private --source=. --remote=github --push`. The existing danserver bare repo must remain the remote named `origin`; the new one must be named `github`. Do not rename, retarget or delete `origin`.
    Set the repository's default Actions token permission to read-only so no future workflow can write to the repository without an explicit per-job grant: `gh api -X PUT repos/danieljamesjohnson/compress_video/actions/permissions/workflow -f default_workflow_permissions=read -F can_approve_pull_request_reviews=false`.
    Push `main` to both remotes and confirm the two remotes hold the same commit.
    Do not create any Actions secret and do not enable any Actions feature beyond the defaults — this phase needs no secret of any kind.
  </action>
  <acceptance_criteria>
    - `gh repo view danieljamesjohnson/compress_video --json visibility -q .visibility` prints `PRIVATE`.
    - `gh repo view danieljamesjohnson/compress_video --json defaultBranchRef -q .defaultBranchRef.name` prints `main`.
    - `gh api repos/danieljamesjohnson/compress_video/actions/permissions/workflow -q .default_workflow_permissions` prints `read`.
    - `git remote` output contains both `origin` and `github`, and `git remote get-url origin` still prints the `danserver.tailc2efd2.ts.net` URL.
    - `git rev-parse main` equals `git rev-parse github/main` and equals `git rev-parse origin/main`.
    - `gh api repos/danieljamesjohnson/compress_video/actions/secrets -q '.total_count'` prints `0`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(gh repo view danieljamesjohnson/compress_video --json visibility -q .visibility)" = "PRIVATE" && test "$(git rev-parse main)" = "$(git rev-parse github/main)"</automated>
  </verify>
  <done>A private GitHub repository exists with a read-only default workflow token, `main` is pushed to both remotes, and `origin` is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Write the toolchain pin baseline, the API-coverage declaration and the phase validation contract</name>
  <read_first>
    - `.planning/phases/01-typed-contract-ci-and-media-info/01-RESEARCH.md` ("Standard Stack" tables — every version with its `[VERIFIED: ...]` provenance)
    - `.planning/phases/01-typed-contract-ci-and-media-info/01-VALIDATION.md` (the template placeholders you are replacing)
    - `.planning/research/PITFALLS.md` (pitfall 1 — toolchain rot is the incumbent's largest complaint cluster; this document is the quarterly baseline)
  </read_first>
  <files>doc/TOOLCHAIN.md, .planning/phases/01-typed-contract-ci-and-media-info/COVERAGE.md, .planning/phases/01-typed-contract-ci-and-media-info/01-VALIDATION.md</files>
  <action>
    Create `doc/TOOLCHAIN.md` as the standing quarterly toolchain-rot baseline. One table, one row per pin, with columns Component / Version / Source of truth / Verified on. Rows and values: Flutter 3.44.1 stable; Dart 3.12.1; JDK temurin/openjdk 17 (record the exact version Task 1 installed); AGP 9.0.1; Kotlin Gradle plugin 2.3.20 applied as `org.jetbrains.kotlin.android`; Gradle (record the wrapper version the example uses); compileSdk 36; minSdk 23; JVM target 17; pigeon 29.0.1; flutter_lints 6.0.0; plugin_platform_interface 2.1.8; kotlinx-coroutines-android 1.10.2; mockito-core 5.18.0; androidx.media3 1.11.0 (not used until Phase 2, pinned here for continuity); GitHub Actions `ubuntu-latest`, `macos-latest` (macOS 26.6.2, Xcode 26.6, CocoaPods 1.17.0); `subosito/flutter-action@v2`; `reactivecircus/android-emulator-runner@v2`; `actions/checkout@v6`; `actions/setup-java@v4`. Add a short header saying the file is re-checked quarterly and that RESEARCH.md's pins expire ~2026-10-15. Add a closing note that the AGP-9 built-in Kotlin path is deliberately NOT used yet: the Flutter 3.44 template still applies Kotlin through the classic external plugin, and moving to the built-in path is Phase 2 (BULD-01) work.
    Create `.planning/phases/01-typed-contract-ci-and-media-info/COVERAGE.md` containing exactly this one line and nothing else: `No external API integration: the phase wraps on-device platform media frameworks (MediaMetadataRetriever / AVFoundation) behind a Pigeon contract; no third-party service, endpoint or SDK is consumed.`
    Replace the placeholders in `.planning/phases/01-typed-contract-ci-and-media-info/01-VALIDATION.md` with the values given verbatim in this plan's `## Validation strategy values` section: the Test Infrastructure table, the Sampling Rate bullets, the full 21-row Per-Task Verification Map, the Wave 0 Requirements list and the Manual-Only Verifications note. Keep the frontmatter keys `status: draft`, `nyquist_compliant: false` and `wave_0_complete: false` as they are — plan 01-07 flips them once every row is green. Leave the Validation Sign-Off checkboxes unticked.
  </action>
  <acceptance_criteria>
    - `grep -c 'Verified on' doc/TOOLCHAIN.md` is at least 1 and `grep -c '3.44.1' doc/TOOLCHAIN.md` is at least 1.
    - `grep -Eq 'AGP|com.android.tools.build' doc/TOOLCHAIN.md` succeeds and `grep -q '9.0.1' doc/TOOLCHAIN.md` succeeds.
    - `grep -q '29.0.1' doc/TOOLCHAIN.md` succeeds (pigeon pin recorded).
    - `wc -l < .planning/phases/01-typed-contract-ci-and-media-info/COVERAGE.md` is `1`.
    - `grep -c 'No external API integration' .planning/phases/01-typed-contract-ci-and-media-info/COVERAGE.md` is `1`.
    - `grep -c '^| 1-0' .planning/phases/01-typed-contract-ci-and-media-info/01-VALIDATION.md` is `21`.
    - `grep -c '{quick command}' .planning/phases/01-typed-contract-ci-and-media-info/01-VALIDATION.md` is `0` (no template placeholder survives).
    - `grep -c 'REQ-{XX}' .planning/phases/01-typed-contract-ci-and-media-info/01-VALIDATION.md` is `0`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '^| 1-0' .planning/phases/01-typed-contract-ci-and-media-info/01-VALIDATION.md)" = "21" && test "$(grep -c 'REQ-{XX}' .planning/phases/01-typed-contract-ci-and-media-info/01-VALIDATION.md)" = "0" && test "$(wc -l < .planning/phases/01-typed-contract-ci-and-media-info/COVERAGE.md)" = "1"</automated>
  </verify>
  <done>`doc/TOOLCHAIN.md`, `COVERAGE.md` and a fully filled `01-VALIDATION.md` are committed, with no template placeholder text remaining.</done>
</task>

</tasks>

## Artifacts this phase produces

Every symbol below is created by phase 1 and does not exist anywhere in the repository beforehand
(the repository contains only planning documents, `README.md`, `QUESTIONS.md`, `.gitignore` and
Mission Control lane config). Treat all of them as new, not as drift from existing code.

- **Dart public API** (`lib/compress_video.dart`, `lib/src/`): `CompressVideo`, `MediaInfo`,
  `CompressVideoException`, `CompressVideoErrorReason`, `reasonFromPlatformCode`.
- **Pigeon contract** (`pigeons/messages.dart`): `MediaInfoMessage`, `ProbeHostApi`,
  `ThumbnailHostApi`.
- **Pigeon-generated** : `lib/src/messages.g.dart`,
  `android/src/main/kotlin/com/danjjohnson/compress_video/Messages.g.kt`,
  `darwin/compress_video/Sources/compress_video/Messages.g.swift`, plus the generated
  `CompressVideoError` error class on both native sides.
- **Kotlin** (`android/src/main/kotlin/com/danjjohnson/compress_video/`): `CompressVideoPlugin`,
  `Probe`, `Thumbnails`, `MediaMath`, `Arguments`. Tests in `android/src/test/kotlin/...`:
  `MediaMathTest`, `ArgumentsTest`.
- **Swift** (`darwin/compress_video/Sources/compress_video/`): `CompressVideoPlugin`, `Probe`,
  `Thumbnails`, `MediaMath`, `Arguments`. Tests in `example/ios/RunnerTests/` and
  `example/macos/RunnerTests/`.
- **Apple packaging**: `darwin/compress_video.podspec`, `darwin/compress_video/Package.swift`.
- **Corpus tooling and fixtures** (`corpus/`): `generate_corpus.sh`, `patch_rotation.py`,
  `verify_corpus.sh`, `sync_to_example.sh`, `README.md`, `portrait_rot90.mp4`, `small_480p.mp4`,
  `noaudio_720p.mp4` and a `*.expected.json` sidecar for each; mirrored to `example/assets/corpus/`.
- **Tests**: `test/compress_video_exception_test.dart`, `test/messages_contract_test.dart`,
  `test/media_info_mapping_test.dart`, `example/integration_test/media_info_test.dart`,
  `example/integration_test/thumbnail_test.dart`.
- **Repo-level files**: `pubspec.yaml`, `analysis_options.yaml`, `.pubignore`,
  `android/build.gradle.kts`, `.github/workflows/ci.yml`, `doc/TOOLCHAIN.md`,
  `.planning/phases/01-typed-contract-ci-and-media-info/COVERAGE.md`, and the whole `example/` app.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → danserver | The Android command-line tools archive and apt packages are downloaded and then executed with the developer's privileges |
| danserver → GitHub | Repository contents, including everything under `.planning/`, become visible to whoever can read the repository |
| GitHub Actions → repository | A workflow's `GITHUB_TOKEN` can mutate the repository if granted write permission |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Android cmdline-tools archive download | medium | mitigate | Resolve the download URL from `developer.android.com` over HTTPS rather than a third-party mirror; record the archive's `sha256sum` in the commit message so a future re-install can be compared against it |
| T-01-02 | Elevation of Privilege | passwordless `sudo apt-get` and `sudo usermod -aG kvm` | medium | mitigate | Sudo is used for exactly two operations — installing `openjdk-17-jdk-headless` and adding `dan` to the `kvm` group. No `chmod` on `/dev/kvm`, no sudo-run SDK tooling, no sudo in any later plan of this phase |
| T-01-03 | Information Disclosure | GitHub repository contents (`.planning/`, `QUESTIONS.md`) | medium | accept | Repository is created **private**; making it public is Dan's explicit decision (QUESTIONS.md #5). Accepted because nothing in `.planning/` is secret — it is design documentation — and the MIT-licensed end state is public by intent |
| T-01-17 | Elevation of Privilege | Actions `GITHUB_TOKEN` default permission | high | mitigate | `default_workflow_permissions=read` set on the repository in Task 2, so no workflow can push, tag or release without an explicit per-job `permissions:` grant |
| T-01-SC-A | Tampering | apt + Google Android SDK repository | low | accept | Both are first-party, HTTPS, signed-by-default distribution channels (Ubuntu archive keyring; `dl.google.com`). No third-party PPA, no JitPack, no curl-pipe-to-shell. Accepted at ASVS L1 as a first-party supply chain on a single-user machine |
</threat_model>

<verification>
1. `bash -lc 'flutter doctor -v'` exits 0 with no `✗` line in the Android toolchain section.
2. `sg kvm -c 'adb devices'` lists one booted emulator in state `device`.
3. `gh repo view danieljamesjohnson/compress_video --json visibility,defaultBranchRef` reports `PRIVATE` / `main`, and `gh api .../actions/permissions/workflow -q .default_workflow_permissions` prints `read`.
4. `git remote` lists both `origin` and `github`; `git rev-parse main github/main origin/main` prints three identical shas.
5. `doc/TOOLCHAIN.md`, `COVERAGE.md` (exactly one line) and `01-VALIDATION.md` (21 task rows, zero placeholders) exist and are committed.
</verification>

<success_criteria>
- Android code can be compiled and run on danserver without any further setup by later plans.
- The repository is on GitHub with a read-only default workflow token and both remotes in sync.
- The phase's toolchain pins, API-coverage declaration and validation contract are recorded.
</success_criteria>

<output>
Create `.planning/phases/01-typed-contract-ci-and-media-info/01-01-SUMMARY.md` when done.
</output>
