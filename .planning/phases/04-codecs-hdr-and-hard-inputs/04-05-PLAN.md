---
phase: 04-codecs-hdr-and-hard-inputs
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - example/integration_test/hard_inputs_test.dart
  - tool/check_parity.sh
  - tool/check_parity_test.sh
  - doc/HARDWARE_CHECKLIST.md
  - doc/PRESETS.md
  - corpus/README.md
  - README.md
  - CHANGELOG.md
  - lib/src/compress_result.dart
  - .planning/REQUIREMENTS.md
  - .planning/phases/04-codecs-hdr-and-hard-inputs/04-VALIDATION.md
autonomous: true
requirements: [TEST-01, CDEC-01, CDEC-02, CDEC-03, AUDO-03]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`hard_inputs_test.dart` emits one `PARITY_JSON` line carrying a record per case whose outcome is genuinely identical on Android, iOS and macOS, and `tool/check_parity.sh` compares the new `toneMapped` and `hevcFallback` fields exactly, the same way it already compares `transmuxed` and `audioReencoded` (D-13)."
    - "Every case deliberately excluded from the parity records — the capability-dependent HEVC and keep-HDR cases above all — is named in `corpus/README.md` with the reason, in the same documented-divergence style 03-08 used for the `truncated_mdat` error buckets. An exclusion is a written decision, never a silent omission."
    - "`tool/check_parity_test.sh` proves both directions for every new field: a matching fixture passes and a flipped one fails naming the field."
    - "`doc/HARDWARE_CHECKLIST.md` exists and lists every check only physical hardware can make — HEVC hardware encode, keep-HDR output, HDR tone-map fidelity by eye, the Dolby Vision profile-8 clip, and the `targetSizeMb`/`estimate()` tolerances from QUESTIONS.md #3 — each with a copy-pasteable command and the exact expected result (D-14)."
    - "The checklist's 'has been run once' status is an explicit, dated Deferred Item pointing at QUESTIONS.md #3 and the Apple device, not a claim — exactly how Phase 3 handled its Mac-only proofs."
    - "`README.md`, `CHANGELOG.md`, `doc/PRESETS.md` and `CompressResult`'s dartdoc describe the real codec and HDR contract, including that `usedOriginal: true` forces every conversion flag false because no conversion produced the delivered file."
    - "`04-VALIDATION.md`'s Per-Task Verification Map carries one row per real task across all five plans, every row has an observed status, and no row is left at its seeded marker."
    - "`REQUIREMENTS.md` marks exactly those Phase 4 requirements the collected evidence actually supports, and says plainly which are left pending and why."
  prohibitions:
    - statement: "MUST NOT emit a parity record for a case whose outcome legitimately differs by device capability. The gate treats a case present on one platform only, or a field that differs, as a failure — feeding it a capability-dependent case would either force a false failure or force a tolerance that hides real regressions."
      status: active
      verification: "The HEVC and keep-HDR case names do not appear in `hard_inputs_test.dart`'s parity accumulator, and `corpus/README.md` names them as deliberate exclusions with the reason."
    - statement: "MUST NOT widen a parity tolerance to make a real cross-platform delta pass. A genuine divergence is documented with its measured values and, if intended, excluded by name."
      status: active
      verification: "Any tolerance touched in `tool/check_parity.sh` names the field it applies to and cites the measurement that justified it, in the script comment and in the summary."
    - statement: "MUST NOT mark a requirement complete on evidence that does not exist. If the macOS runner turned out to be Intel and no HEVC success path ever ran, CDEC-01 stays pending with that stated, exactly as BULD-04 was left pending in 03-09."
      status: active
      verification: "Each requirement marked complete in `REQUIREMENTS.md` is accompanied in the summary by the CI run id or local command output that proves it."
    - statement: "MUST NOT run any `gsd-tools` STATE, ROADMAP or REQUIREMENTS write verb — they edit planning Markdown by whole-file regex and have corrupted all three files in this project before. Hand-edit and `git diff` instead."
      status: active
      verification: "`git diff .planning/REQUIREMENTS.md` shows only the intended checkbox and status-table lines changed, with no blank-line churn elsewhere in the file."
    - statement: "MUST NOT claim the hardware checklist has been run. It cannot be: there is no physical Android phone (QUESTIONS.md #3) and no Apple device."
      status: active
      verification: "`doc/HARDWARE_CHECKLIST.md` carries a dated status line saying it has not been run, and the same item appears in the summary's deferred list."
  artifacts:
    - path: "doc/HARDWARE_CHECKLIST.md"
      provides: "TEST-01's hardware half: every check only a real device can make, with commands and expected results"
      contains: "HEVC"
    - path: "tool/check_parity.sh"
      provides: "toneMapped and hevcFallback compared across all three legs"
      contains: "toneMapped"
    - path: ".planning/phases/04-codecs-hdr-and-hard-inputs/04-VALIDATION.md"
      provides: "The phase's per-task verification accounting, with observed statuses"
      contains: "Per-Task Verification Map"
  key_links:
    - from: "example/integration_test/hard_inputs_test.dart"
      to: "tool/check_parity.sh"
      via: "the PARITY_JSON line CI greps out of each leg's log — the only thing that turns 'the same test ran on three platforms' into 'three platforms observed the same values'"
      pattern: "PARITY_JSON"
    - from: "doc/HARDWARE_CHECKLIST.md"
      to: ".planning/STATE.md deferred items"
      via: "the dated not-yet-run status that keeps an unproven claim visible instead of quietly satisfied"
      pattern: "Deferred"
---

<objective>
Close the phase: make the three CI legs compare what they actually observed, write down what only
real hardware can prove, and account honestly for which requirements this phase earned.

Purpose: TEST-01 has two halves — a corpus that runs in CI, and a documented checklist of what CI
structurally cannot cover. The first four plans built the corpus half. This plan builds the
second half, wires the new suite into the parity gate so cross-platform sameness is measured
rather than asserted, and does the phase's accounting without over-claiming anything that has not
run.
Output: compression parity records for the hard-input cases with documented exclusions, a real
hardware checklist, updated public documentation, a filled validation map, and requirement
statuses backed by named evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-codecs-hdr-and-hard-inputs/04-CONTEXT.md
@.planning/phases/04-codecs-hdr-and-hard-inputs/04-RESEARCH.md
@.planning/phases/04-codecs-hdr-and-hard-inputs/04-01-PLAN.md
@.planning/phases/04-codecs-hdr-and-hard-inputs/04-04-PLAN.md
@corpus/README.md
@.claude/CLAUDE.md
</context>

## Decision map

IDs assigned in 04-01-PLAN.md's decision map; the rows this plan implements:

| ID | 04-CONTEXT.md decision |
|----|------------------------|
| D-13 | All of these cases live in ONE new suite, wired into `tool/run_ios_integration_suites.sh`'s suite list, the Android emulator step, **and the PARITY_JSON compression records**. |
| D-14 | `doc/HARDWARE_CHECKLIST.md` documents the hardware-only checks with exact commands and expected results; "has been run once" is a Deferred Item until a physical Android phone and an Apple device are available. Do not block on it. |
| D-16 | Keep the 90-minute simulator-suite budget; split rather than raise. |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "three platforms are compared on what they observed" — emit, gate, self-test, run</name>
  <read_first>
    - `tool/check_parity.sh` in full (the `PARITY_JSON` merge, the key-set equality check that makes a missing case fatal, the `COMPRESSION_EXACT_FIELDS` list, the `outputBytes` ±50% envelope, the `durationToleranceMs` handling, and the header comment explaining why the `truncated_mdat` error-bucket divergence is deliberately never emitted)
    - `tool/check_parity_test.sh` in full (the hand-written fixture convention and the both-directions proof every new field must follow)
    - `example/integration_test/compress_audio_test.dart` lines 20-45 (the `_compressionParity` accumulator and `_recordCompressionParity`, the per-file copy convention, and the `tearDownAll` that prints one line)
    - `example/integration_test/hard_inputs_test.dart` (every case, now unskipped on all three platforms, and which of them reported identical values on all three legs in 04-04's observed run)
    - `.planning/phases/04-codecs-hdr-and-hard-inputs/04-04-SUMMARY.md` (the per-leg branch outcomes — this is the evidence that decides which cases can be compared at all)
    - `corpus/README.md` (the documented-divergence section and the style 03-08 established)
  </read_first>
  <files>example/integration_test/hard_inputs_test.dart, tool/check_parity.sh, tool/check_parity_test.sh, corpus/README.md</files>
  <action>
    Turn "the same suite ran on three platforms" into "three platforms observed the same values",
    for exactly the cases where that claim is true.

    Add this suite's own `_compressionParity` accumulator and `_recordCompressionParity` helper,
    copied in the per-file style every other suite uses rather than shared, extended with
    `toneMapped`, `hevcFallback` and `videoCodec` alongside the existing fields, and a
    `tearDownAll` printing one `PARITY_JSON` line.

    Choose which cases record, from evidence rather than from hope. Start from 04-04's own
    observed three-leg run: a case records only when all three legs reported identical values for
    the fields being recorded. The unusual-audio and no-audio cases are the strong candidates.
    The HEVC and keep-HDR cases must NOT record — their whole design is that a capable device
    answers differently, and the gate treats any difference as a failure. Judge the two HDR
    tone-map cases and the 4K60 case on the actual evidence: if the three legs agreed, record
    them; if the never-larger outcome or a decode capability differed, exclude them by name.

    Write every exclusion into `corpus/README.md` with its reason and, where there is one, its
    measured values — the same treatment the `truncated_mdat` reason-bucket divergence got. An
    exclusion nobody wrote down is indistinguishable from an oversight six months later.

    Extend `tool/check_parity.sh`'s `COMPRESSION_EXACT_FIELDS` with `toneMapped` and
    `hevcFallback`, and extend its header comment to explain, in the same voice as the existing
    paragraph, why capability-dependent cases are never emitted into a record.

    Extend `tool/check_parity_test.sh` with fixture cases proving both directions for both new
    fields: matching values pass, a flipped `toneMapped` fails naming the field, a flipped
    `hevcFallback` fails naming the field. Demonstrate the teeth once by corrupting a fixture and
    observing the named failure, and record that in the summary.

    Push and watch. The `parity` job must run both of its comparisons — Android against iOS and
    macOS against iOS — green, with the new records included. If a comparison goes red on a real
    delta, investigate and document it; do not widen a tolerance to make it pass.
  </action>
  <acceptance_criteria>
    - `grep -c 'PARITY_JSON' example/integration_test/hard_inputs_test.dart` is at least 1.
    - `grep -c 'toneMapped' tool/check_parity.sh` is at least 1 and `grep -c 'hevcFallback' tool/check_parity.sh` is at least 1.
    - `bash tool/check_parity_test.sh` exits 0 and covers at least four new fixture cases (two fields, both directions).
    - The gate's teeth are demonstrated once: a deliberately flipped fixture makes `check_parity_test.sh` report a failure naming the field, recorded in the summary and reverted.
    - Every case excluded from the records is named in `corpus/README.md` with its reason: `grep -c 'hevc' corpus/README.md` is at least 1 in that section.
    - On an observed CI run: all four jobs conclude `success`, and the `parity` job's two comparisons both pass with the new compression records present in all three artifacts.
    - `cd example && flutter test integration_test -d emulator-5554` exits 0 locally with the emission in place.
  </acceptance_criteria>
  <verify>
    <automated>bash tool/check_parity_test.sh && cd example && flutter test integration_test -d emulator-5554 && cd .. && git push github HEAD:main && git push origin HEAD:main && gh run list --limit 3</automated>
  </verify>
  <reversibility rating="reversible">Parity records and gate fields can be added or removed without touching plugin behaviour; the exclusions are documentation plus a list.</reversibility>
  <done>Three platforms' own observed values for the hard-input cases are compared field by field by a gate that has been proven to fail, and every case that cannot be compared is written down with the reason.</done>
</task>

<task type="auto">
  <name>Task 2: The hardware checklist, and public documentation that matches the code</name>
  <read_first>
    - `corpus/README.md` (the prose style this checklist mirrors: exact command, exact expected result, and the rationale for each)
    - `.planning/phases/04-codecs-hdr-and-hard-inputs/04-CONTEXT.md` → "CI and the hardware checklist" (D-14's exact content list) and → "Deferred Ideas"
    - `.planning/STATE.md` → "Deferred Items" and "Pending Todos" (the phrasing precedent for a dated, resumable deferral, and the existing QUESTIONS.md #3 items about `targetSizeMb` and `estimate()` tolerances on a hardware encoder)
    - `QUESTIONS.md` (#3 for the physical phone and #4 for the real phone clips — the checklist must point at the real entries, not invent numbers)
    - `README.md` (the current codec/HDR wording and the "what this plugin deliberately does not do" framing)
    - `CHANGELOG.md` (the per-phase entry format)
    - `lib/src/compress_result.dart` (the dartdoc on `toneMapped`, `hevcFallback`, `usedOriginal` and `transmuxed`, and the cross-platform sameness wording 03-09 added)
    - `doc/PRESETS.md` (04-03's HEVC section, which this task updates with whatever the macOS leg actually measured, or leaves explicitly unmeasured)
  </read_first>
  <files>doc/HARDWARE_CHECKLIST.md, doc/PRESETS.md, README.md, CHANGELOG.md, lib/src/compress_result.dart, corpus/README.md</files>
  <action>
    Write down the half of TEST-01 that CI structurally cannot cover, and make the public
    documentation say what the code now does.

    Create `doc/HARDWARE_CHECKLIST.md` in `corpus/README.md`'s voice: prose sections, each naming
    the exact command and the exact expected result, not a vague tick list. Cover, at minimum:
    HEVC hardware encode on a physical Android phone and on a real Apple device (the command, and
    the expected `videoCodec: hevc` with `hevcFallback: false`); keep-HDR output on both (expected
    a tone-map flag of false and an output that re-probes as HDR); HDR tone-map fidelity judged by eye
    on a real HDR phone clip, with what "washed out" looks like and what to compare against; the
    Dolby Vision profile-8 case, which needs the real iPhone clip from QUESTIONS.md #4 and cannot
    be authored by ffmpeg; and the `targetSizeMb` and `estimate()` tolerance re-measurements
    QUESTIONS.md #3 has been carrying since Phase 2, with the exact emulator-measured numbers to
    compare against so a hardware run is a comparison rather than a fresh guess.

    Give the file a dated status line stating that it has not been run, why (no physical Android
    phone per QUESTIONS.md #3; no Apple device), and what unblocks it. Mirror that as a Deferred
    Item entry in the summary, in the same shape Phase 3's Mac-only deferrals took, so the
    orchestrator can carry it into STATE.md without inventing wording.

    Update `doc/PRESETS.md`'s HEVC section with whatever the macOS leg genuinely measured in
    04-04, or state plainly that no HEVC bitrate has been measured on any device yet and that the
    checklist owns it. Do not fill the table with plausible numbers.

    Update `README.md` so the codec and HDR behaviour is described honestly: H.264 by default,
    HEVC opt-in and hardware-only with a reported fallback, HDR tone-mapped to SDR by default,
    keep-HDR opt-in with a reported fallback, and unusual audio downmixed or re-encoded rather
    than failing. Add a `CHANGELOG.md` entry for the phase.

    Extend `CompressResult`'s dartdoc so the flag semantics are unambiguous: each flag describes
    the file the caller received, not the request; `usedOriginal: true` forces `transmuxed`,
    `audioReencoded`, `toneMapped` and `hevcFallback` all false because no conversion produced
    that file; and a keep-HDR request answered with `toneMapped: true` means the device could not
    keep HDR. Add the never-larger interaction explicitly: an HDR input whose tone-mapped encode
    would be larger comes back as the original HDR bytes with `usedOriginal: true` and
    a tone-map flag of false — reported, not silent, because CORE-05 is unconditional by design.
  </action>
  <acceptance_criteria>
    - `doc/HARDWARE_CHECKLIST.md` exists, is at least 60 lines, and contains a runnable command for every check: `grep -c '```' doc/HARDWARE_CHECKLIST.md` is at least 8.
    - It names all five check areas: `grep -ci 'hevc' doc/HARDWARE_CHECKLIST.md`, `grep -ci 'keep-hdr\|keephdr' doc/HARDWARE_CHECKLIST.md`, `grep -ci 'dolby' doc/HARDWARE_CHECKLIST.md`, `grep -ci 'targetSizeMb' doc/HARDWARE_CHECKLIST.md` and `grep -ci 'estimate' doc/HARDWARE_CHECKLIST.md` are each at least 1.
    - It carries a dated not-yet-run status line referencing QUESTIONS.md #3.
    - `grep -ci 'usedOriginal' lib/src/compress_result.dart` shows the flag-interaction paragraph present, confirmed by reading the diff.
    - `grep -ci 'hevc' README.md` is at least 1 and `CHANGELOG.md` has a Phase 4 entry.
    - `flutter analyze --fatal-infos --fatal-warnings` exits 0, `PATH=$HOME/development/flutter-stable/bin:$PATH dart format --output=none --set-exit-if-changed lib test` exits 0, and `dart pub publish --dry-run` exits 0.
    - No number in `doc/PRESETS.md`'s HEVC section is unsourced: each is either tied to a named CI run or the section states that nothing has been measured.
  </acceptance_criteria>
  <verify>
    <automated>test -s doc/HARDWARE_CHECKLIST.md && flutter analyze --fatal-infos --fatal-warnings && PATH=$HOME/development/flutter-stable/bin:$PATH dart format --output=none --set-exit-if-changed lib test && dart pub publish --dry-run</automated>
  </verify>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <done>Everything only real hardware can prove is written down with its command and expected result, dated and honestly marked as not yet run, and the public API's documentation matches the behaviour the code actually has.</done>
</task>

<task type="auto">
  <name>Task 3: Phase accounting — fill the validation map, mark only what the evidence supports, sweep clean</name>
  <read_first>
    - `.planning/phases/04-codecs-hdr-and-hard-inputs/04-VALIDATION.md` (the seeded Per-Task Verification Map; every row needs an observed status)
    - `.planning/phases/03-apple-compression-to-parity/03-VALIDATION.md` (the filled precedent: how a green row, a deferred row and a closed-by-a-later-plan row are each worded)
    - `.planning/REQUIREMENTS.md` (the checkbox list and the traceability table — both need updating, and the shared-ID gate means a requirement declared by more than one plan only closes when all its evidence exists)
    - All five `04-0*-SUMMARY.md` files (the actual evidence: CI run ids, local command output, and anything recorded as unresolved)
    - `.claude/CLAUDE.md` (the prohibition on `gsd-tools` planning-file write verbs, and the two-remote push convention)
    - `.planning/phases/03-apple-compression-to-parity/03-09-SUMMARY.md` (the precedent for leaving a requirement pending with a stated reason rather than marking it on partial evidence)
  </read_first>
  <files>.planning/phases/04-codecs-hdr-and-hard-inputs/04-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <action>
    Do the phase's accounting truthfully, and leave the tree green.

    Fill every row of `04-VALIDATION.md`'s Per-Task Verification Map with an observed status
    drawn from the five plan summaries: green with the evidence named (a CI run id or the local
    command that passed), or deferred with the blocker named. Confirm the row count equals the
    real number of tasks across all five plan files — if it does not, fix the table to match
    reality and say which direction the discrepancy went, exactly as 01-01 and 02-01 did when
    their own counts were off by one. No row may remain at its seeded marker.

    Update `REQUIREMENTS.md` by hand — never through a `gsd-tools` write verb, which edits these
    files by whole-file regex and has corrupted `REQUIREMENTS.md`, `ROADMAP.md` and `STATE.md` in
    this project before. Mark complete only what the evidence supports, and for each one name the
    evidence in the summary:
    CDEC-02 and AUDO-03 close on the CI legs that ran them. CDEC-01 and CDEC-03 close only if the
    HEVC and keep-HDR SUCCESS branch actually executed somewhere — on the macOS runner, per
    04-04's observed architecture log; if it did not, leave both pending and say so, the way
    BULD-04 was left pending in 03-09. TEST-01 stays pending regardless, because its own success
    criterion requires the hardware checklist to have been run once and it cannot have been; note
    that its corpus half is complete and the checklist exists.
    After editing, run `git diff .planning/REQUIREMENTS.md` and confirm nothing changed except
    the intended lines — no blank-line churn anywhere else in the file.

    Run the full local sweep on danserver and record each result: `flutter analyze
    --fatal-infos --fatal-warnings`; `flutter test` at the repository root; `dart format` with the
    flutter-stable SDK; `bash corpus/verify_corpus.sh`; `bash tool/check_parity_test.sh`; `bash
    tool/run_ios_integration_suites_test.sh`; `dart pub publish --dry-run`; the Android native
    unit tests; and the full `flutter test integration_test -d emulator-5554` directory on the
    booted emulator. Push both remotes and confirm a final green CI run.
  </action>
  <acceptance_criteria>
    - `awk '/^\| Task ID/,0' .planning/phases/04-codecs-hdr-and-hard-inputs/04-VALIDATION.md | grep -c '^| 4-0'` equals the total `<name>Task ` count across the five plan files, computed as `for f in .planning/phases/04-codecs-hdr-and-hard-inputs/04-0*-PLAN.md; do grep -cE '^[[:space:]]*<name>Task ' "$f"; done | paste -sd+ | bc`.
    - `grep -c '⬜ pending' .planning/phases/04-codecs-hdr-and-hard-inputs/04-VALIDATION.md` is 0.
    - `test -s .planning/phases/04-codecs-hdr-and-hard-inputs/COVERAGE.md` exits 0.
    - `git diff .planning/REQUIREMENTS.md` shows only intended lines changed; running it produces no blank-line churn outside the edited entries.
    - Every requirement marked complete has its evidence (CI run id or command output) named in the summary; every one left pending has its blocker named.
    - The full local sweep passes: analyzer, root `flutter test`, `dart format`, `bash corpus/verify_corpus.sh`, `bash tool/check_parity_test.sh`, `bash tool/run_ios_integration_suites_test.sh`, `dart pub publish --dry-run`, the Android native unit tests, and `flutter test integration_test -d emulator-5554`.
    - A final CI run is green on all four jobs and its run id is recorded.
    - The summary lists the phase's deferred items (the hardware checklist run, the Dolby Vision clip, any Mac-only item) in the shape STATE.md's Deferred Items table expects.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && flutter test && bash corpus/verify_corpus.sh && bash tool/check_parity_test.sh && bash tool/run_ios_integration_suites_test.sh && dart pub publish --dry-run && cd example && flutter test integration_test -d emulator-5554</automated>
  </verify>
  <reversibility rating="reversible">Accounting and documentation; every edit is a planning file under version control.</reversibility>
  <done>The phase's verification map is fully observed, its requirements are marked exactly as far as the evidence reaches, and the whole tree is green locally and in CI.</done>
</task>

</tasks>

## Artifacts this phase produces

See `04-01-PLAN.md` → "Artifacts this phase produces" for the full phase-level list. This plan
contributes: the `_compressionParity` accumulator and `PARITY_JSON` emission in
`hard_inputs_test.dart`; `toneMapped` and `hevcFallback` in `tool/check_parity.sh`'s exact-field
list with matching `tool/check_parity_test.sh` fixtures; `doc/HARDWARE_CHECKLIST.md`; the
documented parity exclusions and divergences in `corpus/README.md`; the HEVC/HDR sections in
`doc/PRESETS.md` and `README.md`; the `CHANGELOG.md` entry; the flag-semantics paragraph in
`CompressResult`'s dartdoc; the filled `04-VALIDATION.md` and the updated `REQUIREMENTS.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI log text → parity gate | The gate's entire input is text scraped from three runner logs; a missing artifact or an empty grep must never read as agreement |
| planning files → automated writers | `REQUIREMENTS.md` and the validation map are edited during this plan, and this project has lost data to regex-based planning-file writers before |
| documentation → future readers | The hardware checklist is the only record of what has NOT been proven; a checklist that reads as done is worse than none |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-23 | Repudiation | A parity gate passing because a case silently did not run on one platform | high | mitigate | The existing key-set equality check is preserved: a case present on only one platform is fatal. New fields are added to the exact-comparison list, and exclusions are by explicit non-emission plus a written reason, never by a loosened comparison |
| T-04-24 | Tampering | A gate that cannot fail, giving false assurance about the new fields | high | mitigate | `check_parity_test.sh` proves both directions for both new fields, and the teeth are demonstrated once by corrupting a fixture and observing the named failure |
| T-04-25 | Repudiation | A requirement marked complete on evidence that never existed | high | mitigate | Each completion must name its CI run id or local command output in the summary; CDEC-01 and CDEC-03 close only if the success branch actually executed; TEST-01 stays pending with its reason stated |
| T-04-26 | Tampering | An automated planning-file writer corrupting `REQUIREMENTS.md` | high | mitigate | Hand-editing only, with `git diff` reviewed for churn outside the intended lines; the prohibition is stated in the plan and in `.claude/CLAUDE.md` |
| T-04-27 | Repudiation | A hardware checklist that reads as satisfied because nobody dated its status | medium | mitigate | A dated not-yet-run status line is an acceptance criterion, and the same item is carried into the summary's deferred list for STATE.md |
| T-04-SC | Tampering | Third-party package supply chain | low | accept | No package from any registry is added in this plan |
</threat_model>

<verification>
1. `hard_inputs_test.dart` emits one `PARITY_JSON` line, the gate compares `toneMapped` and `hevcFallback` exactly, and `bash tool/check_parity_test.sh` proves both directions for both.
2. Every excluded case is named in `corpus/README.md` with its reason.
3. The `parity` job runs both comparisons green on an observed CI run with the new records present in all three artifacts.
4. `doc/HARDWARE_CHECKLIST.md` exists with a runnable command and expected result per check, and a dated status saying it has not been run.
5. `README.md`, `CHANGELOG.md`, `doc/PRESETS.md` and `CompressResult`'s dartdoc describe the real contract, including the never-larger interaction with HDR.
6. `04-VALIDATION.md` has one observed row per real task and none left at the seeded marker.
7. `REQUIREMENTS.md` marks exactly what the evidence supports, with each decision's evidence named in the summary.
8. The full local sweep and a final CI run are both green.
</verification>

<success_criteria>
- Cross-platform sameness for the hard inputs is measured by a gate that has been proven capable of failing, with every exception written down.
- What only real hardware can prove is documented precisely enough to be run in one sitting, and honestly marked as not yet run.
- The phase's requirement statuses are backed by named evidence, and nothing is claimed that did not happen.
</success_criteria>

<output>
Create `.planning/phases/04-codecs-hdr-and-hard-inputs/04-05-SUMMARY.md` when done.
</output>
