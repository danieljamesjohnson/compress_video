---
phase: 04-codecs-hdr-and-hard-inputs
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - darwin/compress_video/Sources/compress_video/Arguments.swift
  - darwin/compress_video/Sources/compress_video/CodecCapabilities.swift
  - darwin/compress_video/Sources/compress_video/CompressionEngine.swift
  - darwin/compress_video/Sources/compress_video/SizeGuard.swift
  - example/ios/RunnerTests/RunnerTests.swift
  - example/macos/RunnerTests/RunnerTests.swift
  - example/integration_test/hard_inputs_test.dart
  - .github/workflows/ci.yml
  - corpus/README.md
autonomous: true
requirements: [CDEC-01, CDEC-02, CDEC-03, AUDO-03]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "iOS and macOS accept `VideoCodec.hevc` and `HdrMode.keepHdr`, and `Arguments.swift`'s two validators mirror `Arguments.kt`'s relaxed versions exactly."
    - "An HDR source compressed with default options on Apple comes back SDR and the result reports `toneMapped: true`, computed from a re-probe of the produced file against the input's own HDR flag — the Phase 3 D-08 reader path (8-bit BGRA, the system tone-maps) is unchanged, only the reporting is new (CDEC-02, D-04)."
    - "`CodecCapabilities.swift` answers 'does this device have a hardware HEVC encoder' through `VTCopySupportedPropertyDictionaryForEncoder` with `kVTVideoEncoderSpecification_RequireHardwareAcceleratedVideoEncoder`, behind an injectable seam so both answers are unit-tested in XCTest with no hardware (D-05)."
    - "HEVC output uses `AVVideoCodecType.hevc` with `AVVideoAverageBitRateKey` and `AVVideoProfileLevelKey` inside `AVVideoCompressionPropertiesKey` — never at the top level — and is pre-flight validated with the same `writer.canApply(outputSettings:forMediaType:)` guard the H.264 dictionary already uses."
    - "Keep-HDR on a capable Apple device reads 10-bit and writes HEVC Main10 with `AVVideoColorPropertiesKey` carrying the source's transfer function (HLG or PQ) and BT.2020 primaries and matrix; on an incapable one it falls back to tone-mapped SDR H.264 reporting `toneMapped: true` and `hevcFallback: true` (CDEC-03, D-07, D-08)."
    - "A source with more than two audio channels, or with non-AAC audio, is re-encoded to at most two AAC channels at 128 kbps on Apple too, reported `audioReencoded: true` — the same engine-side rule Android got in 04-02, not a per-platform variant (AUDO-03)."
    - "`SizeGuard.swift` regains case-for-case parity with `SizeGuard.kt`: `audioChannelCount` on the input and `outputCodecIsHevc` on the options, with the same transmux conditions and mirrored XCTest cases, closing the divergence 04-02 opened deliberately."
    - "Every Android-only skip guard added to `hard_inputs_test.dart` by 04-02 and 04-03 is gone, and the whole suite runs on the iOS simulator and the macOS host."
    - "CI logs the macOS runner's architecture once, so the claim that the macOS host has a hardware HEVC encoder rests on an observed fact rather than on a changelog (04-RESEARCH.md Open Question 3)."
  prohibitions:
    - statement: "MUST NOT place `AVVideoAverageBitRateKey` at the top level of any video output settings dictionary, including the new HEVC one. This exact mistake cost a full CI cycle in 03-04 and the comment recording it is already in the file."
      status: active
      verification: "Reading the diff, every `AVVideoAverageBitRateKey` occurrence is inside an `AVVideoCompressionPropertiesKey` sub-dictionary; the existing warning comment is preserved."
    - statement: "MUST NOT emit HEVC-but-SDR output on a keep-HDR fallback on Apple either — the fallback is tone-mapped SDR H.264."
      status: active
      verification: "The keep-HDR integration case asserts `videoCodec == 'h264'` on the branch where `toneMapped` is true, and the Apple engine resolves its codec and its colour properties from one shared decision."
    - statement: "MUST NOT let `RunnerTests.swift` diverge between `example/ios/` and `example/macos/` — CI diffs them and a divergence fails the job."
      status: active
      verification: "`diff example/ios/RunnerTests/RunnerTests.swift example/macos/RunnerTests/RunnerTests.swift` produces no output."
    - statement: "MUST NOT bypass the unconditional never-larger post-check in `finishJob` for an HDR, HEVC or keep-HDR encode."
      status: active
      verification: "`grep -n 'usedOriginal = tempBytes >= inputBytes' darwin/compress_video/Sources/compress_video/CompressionEngine.swift` still shows the unconditional comparison."
    - statement: "MUST NOT make any part of this plan depend on the MacBook Air being reachable. CI's `apple` job is the verifier; a Mac-only proof is a documented deferred step, never a precondition that halts the phase."
      status: active
      verification: "No task in this plan carries a Mac-dependent precondition, and every acceptance criterion names either a danserver command or a CI observation."
  artifacts:
    - path: "darwin/compress_video/Sources/compress_video/CodecCapabilities.swift"
      provides: "The Apple hardware-HEVC probe behind an injectable seam"
      contains: "VTCopySupportedPropertyDictionaryForEncoder"
    - path: "darwin/compress_video/Sources/compress_video/CompressionEngine.swift"
      provides: "HEVC and keep-HDR writer settings, the forced audio downmix, and real toneMapped/hevcFallback reporting"
      contains: "AVVideoColorPropertiesKey"
    - path: "example/ios/RunnerTests/RunnerTests.swift"
      provides: "Both capability answers and the ported SizeGuard conditions, proven in XCTest"
      contains: "CodecCapabilities"
  key_links:
    - from: "CodecCapabilities.hasHardwareHevcEncoder"
      to: "CompressionEngine's videoOutputSettings"
      via: "the gate that picks the HEVC dictionary over the H.264 one, mirroring Android's independent MIME gate"
      pattern: "AVVideoCodecType.hevc"
    - from: "Probe.getMediaInfo(path:) on the produced file"
      to: "CompressResultMessage.toneMapped"
      via: "buildResult's existing re-probe, compared against the input's own HDR flag — the Apple twin of Android's ExportResult.colorInfo check"
      pattern: "toneMapped"
---

<objective>
Bring the Apple engine to parity on everything Phase 4 has added: HDR reporting, the HEVC opt-in
with a real hardware probe, keep-HDR with a coherent fallback, and unusual audio — and use the
one CI environment that actually has a hardware HEVC encoder to prove the success path nobody
else can.

Purpose: three of this phase's four requirements say "on every platform", and the phase's own
success criteria name the iOS simulator explicitly. The simulator and the Android emulator can
only ever exercise the fallback branch; research found that the `macos-latest` runner is Apple
Silicon and therefore has a Media Engine HEVC encoder, which makes CI's existing macOS desktop
leg the one place the real HEVC and keep-HDR success paths can run without hardware Dan does not
have yet.
Output: an Apple engine at parity, a `SizeGuard.swift` back in lockstep with its Kotlin twin, the
whole `hard_inputs_test.dart` suite unskipped on all three platforms, and an observed CI run
showing which branch each platform took.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-codecs-hdr-and-hard-inputs/04-CONTEXT.md
@.planning/phases/04-codecs-hdr-and-hard-inputs/04-RESEARCH.md
@.planning/phases/04-codecs-hdr-and-hard-inputs/04-PATTERNS.md
@.planning/phases/04-codecs-hdr-and-hard-inputs/04-02-PLAN.md
@.planning/phases/04-codecs-hdr-and-hard-inputs/04-03-PLAN.md
@.planning/phases/03-apple-compression-to-parity/03-CONTEXT.md
@.claude/CLAUDE.md
</context>

## Decision map

IDs assigned in 04-01-PLAN.md's decision map; the rows this plan implements:

| ID | 04-CONTEXT.md decision |
|----|------------------------|
| D-04 | Apple: keep Phase 3's D-08 decision — the reader outputs 8-bit BGRA so the system tone-maps HDR to SDR; assert the result on the simulator. |
| D-05 | Hardware HEVC detection: Apple asks VideoToolbox (`VTCopySupportedPropertyDictionaryForEncoder` with `kVTVideoEncoderSpecification_RequireHardwareAcceleratedVideoEncoder`). |
| D-06 | HEVC fallback is reported through the existing `hevcFallback: true` result field with H.264 output. No new field. |
| D-07 | Keep-HDR output is HEVC Main10 carrying the source's transfer function with BT.2020 primaries, via `AVVideoColorPropertiesKey` + `AVVideoProfileLevelKey`. |
| D-08 | Keep-HDR fallback is tone-mapped SDR H.264 reported as `toneMapped: true` and `hevcFallback: true`. |
| D-09 / D-10 | 5.1 and PCM audio become stereo AAC at 128 kbps, reported `audioReencoded: true` (AVAssetWriter 2-channel AAC). |
| D-13 | Everything lives in the one `hard_inputs_test.dart` suite, wired into `tool/run_ios_integration_suites.sh`. |
| D-17 | On the iOS simulator, keep-HDR and HEVC opt-in assert the fallback path only. |

**Research override to D-17 (04-RESEARCH.md Summary, Pattern 7, Open Question 3):** the macOS CI
host is an Apple Silicon runner whose Media Engine carries a hardware HEVC encoder, so the macOS
leg of the same suite exercises the real success branch. D-17 stays exactly true of the iOS
simulator. No test is written per platform: 04-03's capability-shaped cases already branch on what
the result reports, so the macOS leg proves the other half with no rewrite. Confirm the runner's
architecture in the CI log before relying on it (task 2).

**CI budget discipline for this plan.** Apple verification costs 60–100 minutes per push and the
Mac is unreachable (probed during planning: connection timed out), so CI is the only Apple
verifier. Implement all three tasks locally first — analyzer, `flutter test`, `dart format` with
the flutter-stable SDK, the Android emulator suite, and a careful read of every Apple constant
against 04-RESEARCH.md — then push once and watch. The plan's budget is **three pushed CI attempts
in total, not three per task**; 04-RESEARCH.md assumption A1 explicitly warns that the Apple
constant names were sourced from documentation rather than a local header read, so budget the
first attempt for compile errors. Consolidating the three tasks into fewer commits is acceptable
and precedented (03-02 did exactly this for the same reason); say so in the summary.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "Apple tone-maps an HDR clip and reports it" — plus the SizeGuard port catch-up</name>
  <read_first>
    - `darwin/compress_video/Sources/compress_video/CompressionEngine.swift` in full (the `compress` function: the probe, `resolvePlan`, the transmux branch, `sourceAudioChannelCount`, `audioWillReencode`, the coded/displayed swap, `videoReaderSettings` with its BGRA pixel format and the D-08 reasoning, `videoOutputSettings` with the `AVVideoAverageBitRateKey` warning comment, the `canApply` guards, the copy loop, `finishJob` and `buildResult`)
    - `darwin/compress_video/Sources/compress_video/Probe.swift` lines 190-215 (`isHdr` via `hasMediaCharacteristic(.containsHDRVideo)` and its fallback — the signal both the input flag and the output re-probe come from)
    - `darwin/compress_video/Sources/compress_video/Arguments.swift` lines 225-245 (the two validators to relax, mirroring `Arguments.kt`)
    - `darwin/compress_video/Sources/compress_video/SizeGuard.swift` (the whole port: `InputInfo`, `Options`, `Plan`, `resolve` and the transmux predicate that must regain parity with the Kotlin original)
    - `android/src/main/kotlin/com/danjjohnson/compress_video/SizeGuard.kt` (the Kotlin side as it stands after 04-02 and 04-03 — the two new fields and their exact predicate conditions are what this task ports)
    - `example/ios/RunnerTests/RunnerTests.swift` (the XCTest conventions, the `// MARK:` section layout, and the fact that this file is kept byte-identical with the macOS copy)
    - `example/integration_test/hard_inputs_test.dart` (the cases and skip guards from 04-02 and 04-03)
    - `.planning/phases/03-apple-compression-to-parity/03-CONTEXT.md` → D-08 (the reader-outputs-BGRA decision this plan preserves rather than replaces)
  </read_first>
  <files>darwin/compress_video/Sources/compress_video/Arguments.swift, darwin/compress_video/Sources/compress_video/SizeGuard.swift, darwin/compress_video/Sources/compress_video/CompressionEngine.swift, example/ios/RunnerTests/RunnerTests.swift, example/macos/RunnerTests/RunnerTests.swift, example/integration_test/hard_inputs_test.dart</files>
  <action>
    Get one HDR clip through the Apple engine with an honest result, and close the port
    divergence 04-02 opened, before any new encoder configuration is attempted.

    Relax `Arguments.swift`'s `validateVideoCodec` and `validateHdrMode` to accept `hevc` and
    `keepHdr` exactly as `Arguments.kt` now does, rewriting the doc comments to the real contract.
    These two files are deliberate mirrors; a difference between them is a bug, not a platform
    nuance.

    Port the two `SizeGuard` changes: `audioChannelCount` on `InputInfo` and `outputCodecIsHevc`
    on `Options`, both with defaults so the existing XCTest construction sites compile unchanged,
    plus the two extra transmux conditions. Populate `audioChannelCount` in both `resolvePlan`
    overloads from the channel count the engine already reads out of the audio format
    description, and `outputCodecIsHevc` from the codec decision task 2 adds (for this task it is
    always false, because the HEVC branch does not exist yet — wire the parameter now so task 2
    is a value change rather than another signature change). Mirror the Kotlin unit cases into
    BOTH `RunnerTests.swift` copies, keeping them byte-identical.

    Thread `toneMapped` and `hevcFallback` through `finishJob` into `buildResult` the way
    `audioReencoded`, `transmuxed` and `usedOriginal` already are, and delete the two hardcoded
    `false` literals. `buildResult` already re-probes the destination, so it has the output's own
    `isHdr`: compute `toneMapped` as the input was HDR, the output is not, and `usedOriginal` is
    false. Pass the input's HDR flag down from `compress`, which already holds `inputInfo`.
    `hevcFallback` stays false until task 2 but becomes a threaded value now.

    Do not touch the reader's pixel format in this task. Phase 3's D-08 decision — 8-bit BGRA so
    the system tone-maps — is what already makes Apple's default HDR path correct; this task adds
    the reporting that was missing, not a new mechanism. Say so in a comment so a later reader
    does not "fix" a path that was never broken.

    Remove the Android-only skip guards from the two default-options HDR tone-map cases in
    `hard_inputs_test.dart` so they run on all three platforms, and record in `corpus/README.md`
    whether the two engines agree on the tone-mapped output's characteristics — if they diverge,
    document the measured difference the way 03-08 documented the `truncated_mdat` reason-bucket
    divergence, rather than widening an assertion to cover both.
  </action>
  <acceptance_criteria>
    - `grep -c 'hevc' darwin/compress_video/Sources/compress_video/Arguments.swift` is at least 1 and `grep -c 'keepHdr' darwin/compress_video/Sources/compress_video/Arguments.swift` is at least 1.
    - `grep -c 'audioChannelCount' darwin/compress_video/Sources/compress_video/SizeGuard.swift` is at least 3 and `grep -c 'outputCodecIsHevc' darwin/compress_video/Sources/compress_video/SizeGuard.swift` is at least 2.
    - `grep -v '^\s*//' darwin/compress_video/Sources/compress_video/CompressionEngine.swift | grep -c 'toneMapped: false'` is 0.
    - `diff example/ios/RunnerTests/RunnerTests.swift example/macos/RunnerTests/RunnerTests.swift` produces no output.
    - `flutter analyze --fatal-infos --fatal-warnings` and `flutter test` exit 0 on danserver, and `cd example && flutter test integration_test -d emulator-5554` still exits 0 (the Android side must not regress while the Apple side catches up).
    - On an observed CI run: `XCTest - iOS Runner` and `XCTest - macOS Runner` both succeed with the ported SizeGuard cases, and the iOS simulator leg of `hard_inputs_test.dart` passes the two unskipped tone-map cases reporting `toneMapped: true`.
    - The run id is recorded in the summary.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && flutter test && diff example/ios/RunnerTests/RunnerTests.swift example/macos/RunnerTests/RunnerTests.swift && cd example && flutter test integration_test -d emulator-5554</automated>
  </verify>
  <reversibility rating="costly">The `SizeGuard.swift` port change touches the shared resolver every Apple compression path runs through; reverting means unwinding it in lockstep with the Kotlin side or reopening the divergence.</reversibility>
  <done>Apple reports `toneMapped: true` for an HDR clip that came back SDR, and the two `SizeGuard` ports are identical again.</done>
</task>

<task type="auto">
  <name>Task 2: The Apple hardware-HEVC probe, HEVC output, and keep-HDR Main10</name>
  <read_first>
    - `.planning/phases/04-codecs-hdr-and-hard-inputs/04-RESEARCH.md` → Pattern 6 (the HEVC output-settings dictionary shape, the 10-bit reader pixel format, and the instruction to reuse the existing `canApply` guard), Pattern 7 (the VideoToolbox probe, the simulator's total absence of a hardware encoder, and the Apple Silicon macOS runner fact), Assumptions A1 and A2 (the constant names are documentation-sourced, and the probe's failure mode is inferred — verify both at first compile), and Open Question 3 (log the runner architecture)
    - `.planning/phases/04-codecs-hdr-and-hard-inputs/04-PATTERNS.md` → the `CompressionEngine.swift` section (the `canApply` guard shape and the top-level-bitrate-key warning) and the `CodecCapabilities.swift` section (`ErrorMapping.swift`'s pure shape and the injectable-seam requirement)
    - `darwin/compress_video/Sources/compress_video/ErrorMapping.swift` (the pure-mapping precedent and `knownAVErrorCodes`, including the `-11861 unsupportedOutputSettings` entry a rejected HEVC dictionary is most likely to produce)
    - `android/src/main/kotlin/com/danjjohnson/compress_video/CodecCapabilities.kt` (the Kotlin probe from 04-03 — its pure/wrapper split and KDoc style are what the Swift twin mirrors)
    - `android/src/main/kotlin/com/danjjohnson/compress_video/TransformerEngine.kt` (04-03's shared codec/keep-HDR decision — the Apple side must compute the same decision from one value, not two independent conditions)
    - `.github/workflows/ci.yml` (the `apple` job's steps, including where a one-line architecture log fits before the integration steps)
  </read_first>
  <files>darwin/compress_video/Sources/compress_video/CodecCapabilities.swift, darwin/compress_video/Sources/compress_video/CompressionEngine.swift, darwin/compress_video/Sources/compress_video/SizeGuard.swift, example/ios/RunnerTests/RunnerTests.swift, example/macos/RunnerTests/RunnerTests.swift, example/integration_test/hard_inputs_test.dart, .github/workflows/ci.yml</files>
  <action>
    Give Apple the same two gates Android has, and let the macOS runner prove the branch nothing
    else can.

    Create `CodecCapabilities.swift` mirroring the Kotlin object's shape: a pure function that
    takes an injected "is a hardware encoder available for this codec type" closure and answers
    the product question, plus a default wrapper that supplies the real VideoToolbox lookup —
    `VTCopySupportedPropertyDictionaryForEncoder` with a specification dictionary containing
    `kVTVideoEncoderSpecification_RequireHardwareAcceleratedVideoEncoder` set true for
    `kCMVideoCodecType_HEVC`, treating a non-zero status or a nil dictionary as "no". Import
    `VideoToolbox`. Document the exact return for the exact input in the same doc-comment style
    `ErrorMapping.swift` uses. Assumption A2 says this failure mode is inferred rather than
    verified: handle both a thrown/failed status and an empty result as "no hardware encoder",
    and record what the runner actually did in the summary.

    In `CompressionEngine.compress`, resolve the output codec once, before any settings
    dictionary is built: HEVC when the request asked for HEVC and the probe says yes, or when
    keep-HDR is requested and achievable; H.264 otherwise. Build a parallel
    `hevcOutputSettings` dictionary — `AVVideoCodecKey: AVVideoCodecType.hevc`, the coded
    dimensions, and an `AVVideoCompressionPropertiesKey` sub-dictionary carrying
    `AVVideoAverageBitRateKey`, `AVVideoProfileLevelKey` for HEVC Main10 auto-level and the
    existing expected-source-frame-rate hint. Keep the bitrate key inside the compression
    properties, never at the top level; the comment recording why is already in this file and
    must survive. Guard the chosen dictionary with the same `writer.canApply(outputSettings:forMediaType:)`
    pre-flight the H.264 path uses, and on rejection throw the existing typed `encoderUnavailable`
    error naming HEVC. If `canApply` rejects the HEVC dictionary on a device the probe said was
    capable, that is a real disagreement: fall back to H.264, report `hevcFallback: true`, and
    record it — do not fail the job over a capability mismatch the caller cannot act on.

    For keep-HDR, additionally set `AVVideoColorPropertiesKey` with BT.2020 primaries, the
    source's own transfer function (the HLG constant for an `arib-std-b67` source, the PQ
    constant for `smpte2084`) and the BT.2020 YCbCr matrix, and switch the reader's pixel format
    from the 8-bit BGRA of the default path to the 10-bit biplanar video-range format. Read the
    source's transfer from the asset's format description rather than guessing it from the HDR
    boolean; when it cannot be read, keep-HDR is not achievable and the job takes the fallback.
    Leave the default tone-map path's reader settings exactly as they are.

    Give `hevcFallback` its full Apple meaning, matching 04-03's Android rule exactly: true when
    HEVC was requested and unavailable, or keep-HDR was requested and unachievable, guarded by
    `!usedOriginal`. Populate `SizeGuard.Options.outputCodecIsHevc` from the same resolved
    decision in both `resolvePlan` overloads, so `estimate()` and the real job still agree.

    Add XCTest cases to both `RunnerTests.swift` copies driving the pure capability function with
    an injected yes-closure and an injected no-closure, kept byte-identical between the two files.

    Add one CI step to the `apple` job that logs `uname -m` and `sysctl -n machdep.cpu.brand_string`
    before the integration steps, so the Apple Silicon claim this plan's macOS assertions rest on
    is an observed fact (Open Question 3). If the runner turns out to be Intel, say so plainly:
    the macOS leg then exercises the fallback branch like the others, the capability-shaped tests
    still pass unchanged, and the HEVC success path stays entirely in `doc/HARDWARE_CHECKLIST.md`.

    Remove the Android-only skip guards from the HEVC and keep-HDR cases so they run on all three
    platforms, and record in the summary which branch each of the three legs actually took.
  </action>
  <acceptance_criteria>
    - `darwin/compress_video/Sources/compress_video/CodecCapabilities.swift` exists and `grep -c 'VTCopySupportedPropertyDictionaryForEncoder'` on it is at least 1.
    - Reading the diff: every `AVVideoAverageBitRateKey` occurrence in `CompressionEngine.swift` sits inside an `AVVideoCompressionPropertiesKey` sub-dictionary, and the existing warning comment about the top-level key is unchanged.
    - `grep -c 'AVVideoColorPropertiesKey' darwin/compress_video/Sources/compress_video/CompressionEngine.swift` is at least 1 and `grep -c 'canApply' darwin/compress_video/Sources/compress_video/CompressionEngine.swift` is at least 3 (video H.264, video HEVC, audio).
    - `diff example/ios/RunnerTests/RunnerTests.swift example/macos/RunnerTests/RunnerTests.swift` produces no output.
    - `grep -c 'uname -m' .github/workflows/ci.yml` is at least 1.
    - On an observed CI run: `Build iOS (CocoaPods)`, `Build macOS`, `XCTest - iOS Runner`, `XCTest - macOS Runner` and both SPM builds all succeed; the architecture log line is present and quoted in the summary.
    - The HEVC and keep-HDR cases pass on the iOS simulator (fallback branch) and on the macOS host, and the summary states for each leg which branch ran and what `videoCodec`, `toneMapped` and `hevcFallback` were.
    - `git diff --exit-code pigeons/messages.dart lib/src/messages.g.dart` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && flutter test && diff example/ios/RunnerTests/RunnerTests.swift example/macos/RunnerTests/RunnerTests.swift && git push github HEAD:main && git push origin HEAD:main && gh run list --limit 3</automated>
  </verify>
  <reversibility rating="one-way">The HEVC and keep-HDR output contract becomes observable to callers on Apple; combined with 04-03's Dart-side relaxation, re-restricting it later is a breaking change.</reversibility>
  <done>Apple picks HEVC only where VideoToolbox says a hardware encoder exists, writes Main10 with BT.2020 colour properties for keep-HDR, falls back to tone-mapped SDR H.264 otherwise, and CI shows which branch each platform took against an observed runner architecture.</done>
</task>

<task type="auto">
  <name>Task 3: Apple audio that is not stereo AAC, the 4K60 source, and the last skip guard gone</name>
  <read_first>
    - `darwin/compress_video/Sources/compress_video/CompressionEngine.swift` lines 105-175 and 250-330 (`sourceAudioChannelCount`, `inputAudioCodec`, `sourceIsAAC`, `audioWillReencode` and its three downstream consumers; the `aacOutputSettings` dictionary with its per-channel bitrate floor, `AVEncoderBitRateStrategyKey` and `AVChannelLayoutKey`, and the reasoning comments behind each — all three were paid for with CI cycles in 03-05)
    - `android/src/main/kotlin/com/danjjohnson/compress_video/TransformerEngine.kt` (04-02's forced-downmix decision and its 128 kbps constant — the Apple rule must be the same rule, not a parallel invention)
    - `.planning/phases/04-codecs-hdr-and-hard-inputs/04-RESEARCH.md` → Pitfall 1 (the exact Apple condition to extend and why the target of two must be an engine-side default) and Pitfall 5 (the PCM-in-MP4 container risk, and the `.mov` contingency)
    - `example/integration_test/compress_audio_test.dart` (the `esds` AudioSpecificConfig channel read, and why the `mp4a` sample entry's `channelcount` is unreliable on Apple specifically — Apple's muxer writes 2 there regardless)
    - `example/integration_test/hard_inputs_test.dart` (the audio and 4K60 cases from 04-02 and their remaining skip guards)
    - `corpus/README.md` (where a documented cross-platform divergence goes, and the format 03-08 used for the `truncated_mdat` reason buckets)
  </read_first>
  <files>darwin/compress_video/Sources/compress_video/CompressionEngine.swift, example/integration_test/hard_inputs_test.dart, corpus/README.md</files>
  <action>
    Close AUDO-03 on Apple with the same rule Android got, then take the last skip guards out and
    prove the whole suite on all three platforms.

    Extend `audioWillReencode` so it is also true when the source has more than two channels,
    alongside the existing "the request says re-encode" and "the source is not AAC" conditions.
    For the two engine-forced cases (non-AAC source, or more than two channels) under a
    passthrough request, resolve the target channel count to the source's own count capped at two
    — a mono non-AAC source must not be upmixed — and the target bitrate to the same 128 kbps
    constant Android uses rather than `plan.audioBitrateBps`, because a 5.1 track's own bitrate
    describes six channels and an LPCM track's describes raw samples; neither is a sensible thing
    to ask an AAC encoder for. Leave the explicit-re-encode branch resolving exactly as it does
    today (the caller's validated channel count and `plan.audioBitrateBps`), and leave the
    per-channel bitrate floor, the constant bitrate strategy and the explicit channel layout in
    place — all three are load-bearing and were expensive to find.

    Remove the remaining Android-only skip guards from the 5.1, PCM, no-audio and 4K60 cases so
    the whole suite runs unskipped everywhere, and confirm no skip guard remains anywhere in the
    file.

    Watch what the two Apple legs do with the PCM fixture specifically. 04-RESEARCH.md flags that
    `AVAssetReader`'s acceptance of an ffmpeg-authored LPCM-in-MP4 track is unverified. If it is
    rejected in a way attributable to the container rather than to LPCM itself, switch that one
    fixture to a `.mov` container in 04-01's generation script, regenerate, re-sync and update the
    references — that contingency is pre-authorised and needs no new decision, but it does need
    to be recorded.

    Record any genuine Android-versus-Apple divergence this task surfaces in `corpus/README.md`
    with its measured values, in the same style 03-08 used for the error-bucket divergence:
    documented and, where appropriate, deliberately excluded from the parity records rather than
    forced to agree. 04-05 consumes that decision when it writes the parity schema.
  </action>
  <acceptance_criteria>
    - `grep -c '128000' darwin/compress_video/Sources/compress_video/CompressionEngine.swift` is at least 1, as a named static constant rather than an inline literal at a call site.
    - Reading the diff: `audioWillReencode` has three conditions, and the forced-path target channel count is the source's own count capped at two.
    - `grep -c 'Platform.isAndroid' example/integration_test/hard_inputs_test.dart` is 0.
    - `grep -c 'skip:' example/integration_test/hard_inputs_test.dart` is 0.
    - On an observed CI run: the `apple` job concludes `success` with `hard_inputs_test.dart` passing on both the iOS simulator leg and the macOS leg, including the 5.1, PCM, no-audio and 4K60 cases.
    - `cd example && flutter test integration_test -d emulator-5554` exits 0 on danserver, with every case unskipped.
    - The produced 5.1 output on Apple reads two channels from its own `esds` descriptor, not from the `mp4a` sample entry.
    - Any cross-platform divergence found is written into `corpus/README.md` with measured values, and the summary names it.
    - The plan's total pushed CI attempts are at most three; if the third attempt is still red, stop, write the summary with `status: partial`, and record exactly what remains.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --fatal-infos --fatal-warnings && flutter test && cd example && flutter test integration_test -d emulator-5554 && cd .. && git push github HEAD:main && git push origin HEAD:main && gh run list --limit 3</automated>
  </verify>
  <reversibility rating="costly">Changing the forced-re-encode target for the existing non-AAC passthrough path alters behaviour Phase 2 and 3 shipped (it previously kept the source's channel count and bitrate); reverting means re-diverging from Android.</reversibility>
  <done>Unusual audio and a 4K60 source compress on iOS and macOS as well as Android, the whole suite runs unskipped on all three platforms, and any real divergence is written down rather than smoothed over.</done>
</task>

</tasks>

## Artifacts this phase produces

See `04-01-PLAN.md` → "Artifacts this phase produces" for the full phase-level list. This plan
contributes: `CodecCapabilities.swift` and its XCTest cases in both `RunnerTests.swift` copies;
the relaxed validators in `Arguments.swift`; the `audioChannelCount` and `outputCodecIsHevc` port
in `SizeGuard.swift`; the HEVC and keep-HDR writer settings, the 10-bit keep-HDR reader format,
the forced audio downmix and the threaded `toneMapped`/`hevcFallback` in `CompressionEngine.swift`;
the removal of every skip guard in `hard_inputs_test.dart`; the runner-architecture log step in
`.github/workflows/ci.yml`; documented cross-platform divergences in `corpus/README.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller request → Swift validators | `videoCodec`/`hdrMode` strings reach `Arguments.swift`, the Apple-side authority for any caller that bypasses the Dart enum |
| untrusted HDR/PCM/5.1 media → AVFoundation | 10-bit HEVC, LPCM-in-MP4 and 6-channel AAC tracks reach `AVAssetReader` for the first time |
| VideoToolbox capability answer → writer configuration | A wrong "yes" configures an encoder the device cannot drive |
| repository → hosted macOS runner | Every Apple proof in this plan is an observation of a CI job, not of a machine under our control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17 | Denial of Service | A malformed or truncated HDR/LPCM/5.1 stream crashing `AVAssetReader` rather than failing typed | high | mitigate | Every new branch routes through the existing `Self.mapToCompressVideoError` funnel or the existing direct typed-error construction used by the H.264 `canApply` guard; no new unguarded AVFoundation call is introduced, and `ErrorMapping.swift`'s table already covers the plausible codes including `-11861 unsupportedOutputSettings` |
| T-04-18 | Spoofing | A VideoToolbox capability answer that disagrees with what the encoder will actually accept | high | mitigate | The probe's answer is a hint, not a promise: the chosen settings dictionary is still pre-flight validated by `writer.canApply(..)`, and a rejection falls back to H.264 with `hevcFallback: true` rather than failing the job or shipping a file the device could not really produce |
| T-04-19 | Tampering | An unrecognised `videoCodec`/`hdrMode` string reaching the Apple engine through the generated host API | medium | mitigate | `Arguments.swift` widens by exactly two values and keeps rejecting everything else through the same chain; the relaxation is mirrored case-for-case with `Arguments.kt` |
| T-04-20 | Tampering | `AVVideoAverageBitRateKey` re-introduced at the top level of the new HEVC dictionary | high | mitigate | The existing warning comment is preserved, the acceptance criteria require reading the diff for every occurrence of the key, and `canApply` would reject the dictionary at runtime anyway — which is exactly how this was found the first time |
| T-04-21 | Repudiation | An Apple claim resting on a runner property nobody observed | medium | mitigate | A CI step logs `uname -m` and the CPU brand string before the integration steps, and the summary quotes it; if the runner is Intel the plan says so and moves the HEVC success proof to the hardware checklist |
| T-04-22 | Information Disclosure | A failed HEVC or keep-HDR attempt leaving a partial file behind | medium | mitigate | The existing `finishJob`/cancel paths delete the temp file on every terminal path and are unchanged; a `canApply` rejection happens before any writer session starts |
| T-04-SC | Tampering | Third-party package supply chain | low | accept | No package from any registry is added; AVFoundation and VideoToolbox are system frameworks already linked |
</threat_model>

<verification>
1. `Arguments.swift` accepts the two new values and still rejects anything else, mirroring `Arguments.kt` exactly.
2. `SizeGuard.swift` and `SizeGuard.kt` are case-for-case identical again, proven by the mirrored XCTest and JVM cases both passing.
3. An HDR clip compressed with default options on the iOS simulator and the macOS host reports `toneMapped: true` with a non-HDR output.
4. `CodecCapabilities.swift` answers both ways under injected closures in XCTest on both Apple platforms, and the real probe's answer on each CI leg is recorded.
5. HEVC and keep-HDR cases pass on all three platforms with the branch each took recorded, against an observed runner architecture.
6. 5.1, PCM, no-audio and 4K60 all compress on both Apple platforms, with the 5.1 output reading two AAC channels from its own `esds`.
7. No skip guard remains in `hard_inputs_test.dart`, and the whole directory passes on the Android emulator locally and on both Apple legs in CI.
</verification>

<success_criteria>
- Every Phase 4 behaviour exists on iOS and macOS as well as Android, from one shared Swift core.
- The one CI environment with a hardware HEVC encoder is used to prove the success branch, and the claim that it has one is an observed fact.
- No Apple proof in this plan depended on the MacBook Air being awake.
</success_criteria>

<output>
Create `.planning/phases/04-codecs-hdr-and-hard-inputs/04-04-SUMMARY.md` when done.
</output>
